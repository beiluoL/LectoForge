# KnowFlow 学习工作台 · 技术架构与功能手册

> 版本：v1.1.0 ｜ 文档日期：2026-08-07 ｜ 适用平台：macOS (arm64)
> 本文档面向开发者与维护者，覆盖技术架构、数据模型、接口契约、功能说明、构建发布与踩坑记录。
>
> **v1.1.0 更新摘要**（对应提交 `f03310c` → `b0d7048` → `75a7987`）：
> ① 新增**间隔复习系统** `/review`（跨表 SRS，§7.12）；② 新增**全局命令面板** `Cmd/Ctrl+K`（§7.13）；
> ③ **工作台首页动态化**（真实统计接口 `/api/dashboard/stats`，§7.14）；④ 新增**新手引导 + 设置中心**（§7.15）；
> ⑤ 宿主侧车升级为 **`SidecarManager` 无限自愈重启**（§9.4）；⑥ 前端新增**断线自动重连**（§6.6）；
> ⑦ 数据目录改由宿主经 **`KNOWFLOW_DATA_DIR`** 环境变量注入（§3.1）。
> 端点 87 → **111**（v1.1.0 为 93，本次收集箱 `/api/inbox` 新增 8 个，其后「5 大体验升级」再增 `/api/inbox/metadata` GET+POST 共 2 个；2026-08-07 番茄钟 `/api/pomodoro` 再增 5 个）；前端业务视图 14 → **20**，路由配置 16 → **24**（新增 `/inbox` + `/workbench/capture`→`/inbox` 重定向 + 番茄钟 `/pomodoro` / `/pomodoro/stats`）。

---

## 目录

1. [产品定位与形态](#1-产品定位与形态)
2. [技术架构](#2-技术架构)
3. [数据层设计](#3-数据层设计)
4. [后端接口手册](#4-后端接口手册)
5. [核心算法](#5-核心算法)
6. [前端架构](#6-前端架构)
7. [功能手册（按模块）](#7-功能手册按模块)
8. [AI 能力体系](#8-ai-能力体系)
9. [桌面外壳原生能力](#9-桌面外壳原生能力)
10. [开发、构建与发布](#10-开发构建与发布)
11. [约定与规范](#11-约定与规范)
12. [已知限制与踩坑清单](#12-已知限制与踩坑清单)

---

## 1. 产品定位与形态

**KnowFlow 学习工作台**是一款 **macOS 本地优先**的学习闭环桌面应用，把 Web 项目中的「学习工作台」模块独立成单机应用。

核心方法论是一条**四阶段学习闭环**：

```
收集箱（Capture）  →  康奈尔笔记（Note）  →  记忆强化  →  费曼故事（Story）
   信息入口              结构化加工          ├ 间隔重复 SM-2        输出验证
                                            ├ 记忆宫殿 Loci
                                            └ 主动回忆（三轮闭卷）
```

产品特征：

| 维度 | 说明 |
|------|------|
| 数据主权 | 全量数据存本机 SQLite，不上云、不需要账号、不需要登录 |
| 离线可用 | 除 AI 增强外全部功能离线可用；AI 未配置时整体优雅降级 |
| 单用户 | 后端硬编码 `CURRENT_USER = 1`，无鉴权体系 |
| 契约兼容 | 接口与字段命名与 Web 端 `Wb*` 实体逐字段对齐，支持从 Web 端导入数据 |

---

## 2. 技术架构

### 2.1 技术栈总览

| 层次 | 技术选型 | 版本 | 选型理由 |
|------|---------|------|---------|
| 桌面外壳 | **Tauri 2**（Rust） | 2.x | 极薄壳 + macOS 原生 WKWebView，体积/内存远小于 Electron |
| 后端运行时 | **Node.js**（侧车进程） | 22 | 复用 npm 生态，原生 `fetch` 免 SDK |
| Web 框架 | **Fastify** | ^4.28 | 轻量、插件化、性能好 |
| 数据库 | **SQLite**（better-sqlite3，WAL） | ^11.3 | 单文件本地库，同步 API 无回调地狱 |
| ORM | **Drizzle ORM** | ^0.33 | 类型安全 SQL 构造器，接近手写 SQL |
| 前端框架 | **Vue 3**（Composition API + `<script setup>`） | ^3.4 | — |
| 构建工具 | **Vite** | ^5.4 | — |
| 路由 | **vue-router**（history 模式） | ^4.4 | 同源托管，无需 hash 模式 |
| 样式 | **Tailwind CSS + 自定义 CSS 变量** | ^3.4 | token 单一来源，见 §6.3 |
| HTTP 客户端 | **axios** | ^1.7 | 统一响应拦截解包 |
| 导出能力 | **html2canvas + jsPDF** | — | 笔记导出 PNG / A4 多页 PDF |
| 语言 | **TypeScript** 全栈 | ^5.5 | — |
| Rust 侧依赖 | tauri-plugin-shell / notification / updater、ureq、serde | 2.x | `ureq` 为纯 Rust HTTP，无 OpenSSL 依赖 |

**状态管理**：已引入 **Pinia 4 + pinia-plugin-persistedstate 4**。既有 `utils/toast.ts` 的 `reactive` 单例与 `views/DocLibrary/useDocStore` 等局部状态模式保留；**新模块跨组件共享状态一律用 Pinia**。当前 store 清单：

| Store | 文件 | 职责 | 持久化 |
|-------|------|------|--------|
| `useMemoryPalaceStore` | `store/memoryPalace.ts` | 记忆宫殿位点、漫游进度、熟练度 | `lociList` / `currentTourIndex` / `masterLevels`（key `kf:memory-palace`；palaces / activePalaceId 走后端） |
| `useAppStore` | `store/appStore.ts` | 引导状态 `hasOnboarded` + 应用设置（dataDir / AI 网关参数） | 仅引导状态与非敏感设置；**明文 `apiKey` 绝不入 localStorage**（`persist.pick` 排除） |
| `useDashboardStore` | `store/dashboardStore.ts` | 工作台首页聚合统计 | 否（每次进页拉取） |
| `useReviewStore` | `store/reviewStore.ts` | 间隔复习队列（SM-2 翻卡）+ 复习驾驶舱看板（热力图/遗忘曲线/旧系统计数） | 否 |
| `useSearchStore` | `stores/searchStore.ts` | 全局命令面板开合、查询词、结果 | 否 |

> 注意目录不一致：`searchStore.ts` 位于 `src/stores/`（复数），其余在 `src/store/`（单数）。新增 store 请统一放 `src/store/`。

**图标体系**：`ui/Icon.vue` 已重构为 **lucide-vue-next 包装器**——传入合法 lucide 图标名（如 `map-pin` / `brain` / `route`，支持 kebab/snake→PascalCase 自动解析）即渲染，未命中时回退到内置 220+ 手写 SVG 兜底；新代码统一用 lucide 图标名，无需手写 SVG。

**思维导图 / 渲染依赖**：`@vue-flow/core`（流程图）、`markmap-lib` / `markmap-view`（导图）、`d3-flextree`（流程图树布局）、`highlight.js`（代码高亮）已纳入前端依赖。

### 2.2 进程与通信拓扑

```
┌─────────────────────────── macOS .app ────────────────────────────┐
│                                                                    │
│  ┌──────────────────┐   spawn(sidecar)   ┌──────────────────────┐ │
│  │  Tauri 宿主 (Rust)│ ─────────────────→ │  Node 侧车 (server)  │ │
│  │  · 端口协商       │  env:KNOWFLOW_    │  · Fastify           │ │
│  │  · SidecarManager │      DATA_DIR      │  · better-sqlite3    │ │
│  │    (监控+自愈重启) │ ←── /api/health ──│  · 同源托管 web/     │ │
│  │  · 原生菜单/通知  │  crash→退避重启   │  · 孤儿自检(ppid=1)  │ │
│  │  · 自动更新       │  exit→SIGTERM/KILL └──────────┬───────────┘ │
│  └────────┬─────────┘                                │             │
│           │ 创建窗口                                  │ 监听        │
│           ▼                                          ▼             │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  WKWebView  →  http://127.0.0.1:<port>                      │   │
│  │  Vue SPA（同源请求 /api/*，零 CORS、history 路由可用）        │   │
│  └────────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼ （仅 AI 功能，用户显式配置后）
                        OpenAI 兼容网关（DeepSeek / OpenAI / SiliconFlow）
```

**关键设计**：Node 后端**同源托管**前端 `dist` 与 `/api/*`，窗口直接加载 `http://127.0.0.1:<port>`。由此：

- 零 CORS 配置负担
- vue-router 可用 history 模式（无 `#`）
- 无需 WebSocket 或 Tauri IPC 承载业务数据，业务代码与 Web 端几乎零差异

### 2.3 目录结构

```
desktopApp/
├── package.json                 # 编排脚本（dev:all / build:all / tauri:*）
├── README.md                    # 快速开始与构建说明
├── AI功能增强方案.md             # AI 能力的分阶段规划文档（P0~P3）
├── 技术架构与功能手册.md          # 本文档
├── binaries/server              # Node 侧车二进制（由 scripts/prepare-bin.sh 生成）
├── scripts/
│   ├── prepare-bin.sh           # 复制本机 node → binaries/server-<target-triple>
│   └── gen_icon.py              # 生成应用图标
│
├── src-api/                     # ── Node 后端 ──
│   ├── src/index.ts             # Fastify 启动、响应信封、静态托管、SPA 回退、孤儿自检、/api/health
│   ├── src/db/
│   │   ├── index.ts             # 建库、WAL、幂等 DDL、增量补列（含 SRS 列）、预置分类
│   │   └── schema.ts            # Drizzle 表定义（10 张表）
│   ├── src/routes/              # overview / captures / notes / reviews / palaces / recall
│   │                            # / stories / categories / migration / ai / library / mindmap
│   │                            # ★ review（跨表间隔复习）/ search（全局搜索）
│   │                            # ★ dashboard（首页统计）/ config（引导与应用配置）
│   ├── src/services/sm2.ts      # SM-2 算法（与 Web 端逐位一致；新旧复习系统共用）
│   ├── src/lib/
│   │   ├── paths.ts             # 数据目录五级回退 + 各类文件/子目录路径的唯一收敛点
│   │   ├── llm.ts               # LLM 配置中心 + OpenAI 兼容调用 + embedding
│   │   ├── vault.ts             # 文档库 vault 根目录与工作区配置
│   │   ├── mindmapStore.ts      # 思维导图文件存储
│   │   └── prompts.ts           # 15 组 Prompt 模板与输出类型（882 行）
│   └── data/                    # 开发态数据目录（.gitignore，含 workbench.db、ai-config.json、config.json）
│
├── src-ui/                      # ── Vue 3 前端 ──
│   ├── src/main.ts / App.vue    # 挂载、顶栏布局、ToastHost、CommandPalette、ConnectionOverlay
│   ├── src/router/index.ts      # 19 条业务路由 + / 重定向 + /workbench/capture 重定向 + /:pathMatch 兜底 = 24 条配置（含 2026-08-07 新增 /pomodoro、/pomodoro/stats）；含引导门禁守卫
│   ├── src/api/                 # request.ts（axios 实例 + 断线重连拦截）/ workbench.ts / ai.ts
│   │                            # ★ review.ts / search.ts / dashboard.ts / config.ts / types.ts
│   ├── src/components/          # layout/DesktopTopNav、ui/Icon、ui/ToastHost、AiAssociatePanel
│   │                            # ★ CommandPalette.vue、ui/ConnectionOverlay.vue
│   ├── src/store/               # Pinia：memoryPalace / appStore / dashboardStore / reviewStore
│   ├── src/stores/              # Pinia：searchStore（历史遗留的复数目录，见 §2.1 注）
│   ├── src/utils/               # toast.ts、★ connection.ts（连接状态机 + 指数退避重连）
│   ├── src/views/               # 18 个业务视图（记忆宫殿/文档库/思维导图/★Review/★Onboarding/
│   │                            # ★Settings 为目录视图）+ workbench-shared.css + ai-shared.css
│   └── src/style.css            # 设计 token 单一来源（--kb-*，1427 行）
│
└── src-tauri/                   # ── Tauri 2 外壳 ──
    ├── src/lib.rs               # SidecarManager（拉起/监控/自愈重启/回收）、端口协商、健康检查、
    │                            # 菜单、通知、更新、restart_sidecar 命令（760 行）
    ├── tauri.conf.json          # 打包资源、externalBin、插件配置
    ├── capabilities/default.json# 权限：shell spawn/execute、notification、updater、dialog
    └── Cargo.toml
```

> 标 ★ 的为 v1.1.0 新增文件。

### 2.4 启动链路

**开发态**（`npm run dev:all`，无需 Rust）：

```
Vite dev server :5173 ──/api 代理──→ Node tsx watch :8787 ──→ src-api/data/workbench.db
浏览器直接访问 http://localhost:5173
```

**生产态**（双击 .app）：

```
1. Tauri setup()
2. resolve(BaseDirectory::AppData) → create_dir_all      解析 ~/Library/Application Support/com.knowflow.desktop/
3. pick_free_port(8787)                                  从 8787 起探测可绑定端口（最多 +20）
4. 构造 SidecarSpec { node_bin, entry, web_dir, data_dir, port, log_file }
5. Arc<SidecarManager>::supervise()                      独立线程：spawn → wait → 异常则退避重启（无限）
   └ spawn 时注入 env KNOWFLOW_DATA_DIR=<data_dir> + 参数 --data-dir <data_dir>
6. wait_for_backend(p)                                   轮询 /api/health，校验 service == "knowflow-desktop-api"，最长 ~15s
7. 创建 WebviewWindow → http://127.0.0.1:<p>              （1200×800）
8. 挂载原生菜单 + 启动复习提醒后台线程
9. RunEvent::Exit → manager.shutdown()                   SIGTERM，2s 内未退再 SIGKILL，零孤儿残留
```

**端口协商为什么必要**：后端遇 `EADDRINUSE` 会自行 `+1` 漂移。若宿主写死 8787，一旦上次实例没退干净，窗口会连到**陈旧服务**上（它托管的是旧构建产物），页面卡在加载态。因此端口由宿主先探测再传给侧车，两侧达成一致。

**宿主模式下端口绝不漂移**（v1.1.0 新增约束）：侧车通过 `isLaunchedByHost()`（检测 `KNOWFLOW_DATA_DIR` 环境变量或 `--data-dir` 参数）判断自己是被宿主拉起的。此时遇到端口占用**不再 +1 漂移**，而是等待旧端口释放（最多 30 次 × 200ms）后复用同一端口。原因：崩溃重启后若端口漂移，已加载页面的 origin 就失联了，前端断线重连会永远探不到后端。独立运行（`npm run dev:api`）时保留 +1 漂移行为。

**健康检查为什么不能只探 TCP**：端口上可能坐着别的程序。必须校验响应体里的 `service` 标识，确认是自己刚拉起的那个后端；后端还带 `bootId`（`pid-时间戳`）用于区分实例——前端据此判断"后端换了新实例"（见 §6.6）。

`GET /api/health` 完整返回：

```jsonc
{
  "status": "ok",
  "service": "knowflow-desktop-api",
  "bootId": "17061-1754500000000",  // pid-启动时间戳，实例唯一标识
  "pid": 17061,
  "uptimeMs": 128340,
  "dataDir": "/Users/x/Library/Application Support/com.knowflow.desktop",
  "webDir": "/Applications/KnowFlow.app/Contents/Resources/web"
}
```

---

## 3. 数据层设计

### 3.1 存储位置

数据目录解析全部收敛在 `src-api/src/lib/paths.ts` 的 `resolveDataDir()`，采用**五级回退 + 可写性实证**策略（v1.1.0）：

| 优先级 | 来源 | 典型场景 |
|--------|------|---------|
| 1 | 环境变量 **`KNOWFLOW_DATA_DIR`** | **生产**：Tauri 宿主用 `BaseDirectory::AppData` 解析后注入（权威来源） |
| 2 | 命令行参数 `--data-dir` | 手工调试侧车 |
| 3 | `<src-api>/data` | 开发态（已加入 `.gitignore`） |
| 4 | 系统 AppData 目录 | 独立运行且前三者不可用 |
| 5 | `os.tmpdir()` | 最后兜底，保证进程不因无处落盘而崩溃 |

**关键点**：每级候选都要过 `fs.mkdirSync` + `fs.accessSync(dir, W_OK)` 的**实证可写性校验**，不可写立即降级到下一级。这是因为 `.app` 包内目录只读，若把 SQLite 打开在包内路径，`better-sqlite3` 会在启动瞬间崩溃且难以定位。

目录内文件（均由 `paths.ts` 的具名函数产出，业务代码不得自行拼路径）：

| 文件/目录 | 产出函数 | 说明 |
|-----------|---------|------|
| `workbench.db`（+ `-wal` / `-shm`） | `getDbPath()` | 主数据库，可用 `KNOWFLOW_DB` 单独覆盖为任意绝对路径（便于跑测试库） |
| `ai-config.json` | `getAiConfigPath()` | AI 网关配置，写盘后 `chmod 600`，含 API Key |
| `config.json` | `getAppConfigPath()` | **v1.1.0 新增**：应用外壳配置（`hasOnboarded` / `dataDir` 偏好），**不含任何密钥** |
| `workspace.json` | `getWorkspaceConfigPath()` | 文档库 vault 根目录记忆 |
| `mindmaps/` | `getMindmapsDir()` | 思维导图文件 |
| `uploads/` | `getUploadsDir()` | 用户上传图片 |
| `logs/sidecar.log` | `getLogsDir()` | 侧车 stdout/stderr 转发日志，>4MB 自动重开 |

> **配置职责边界**：`config.json`（应用外壳）与 `ai-config.json`（AI 密钥）刻意分离。引导页/设置中心提交的 AI 参数会经 `llm.saveConfig()` 落到 `ai-config.json`，从而真正驱动全局 AI 能力，而不是变成一份与实际调用脱节的死数据。

**SQLite 运行参数**：

```sql
PRAGMA journal_mode = WAL;    -- 并发与崩溃安全
PRAGMA foreign_keys = OFF;    -- 逻辑外键，关系完整性由应用层保证
```

### 3.2 数据表清单（11 张）

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| `wb_pomodoro_log` | ★ 番茄钟时段流水（2026-08-07 新增） | `user_id`(默认 1)、`start_time`、`end_time`、`type`('work'\|'short_break'\|'long_break')、`duration_seconds`、`created_at`；`idx_wb_pomodoro_end(end_time)` 加速按天聚合 |
| `categories` | 本地分类（替代线上 doc_category） | `name`、`parent_id`、`sort`；首次运行预置「未分类/工作/学习/生活」 |
| `wb_capture` | 模块一 · 收集箱 | `title`、`content`、`source_type`(MANUAL/DOC/WEB/AI/IMPORT)、`source_url`、`tags`、`status`(INBOX/PROCESSED/ARCHIVED)、`starred` |
| `wb_note` | 模块二 · 康奈尔笔记 | `cue_column`（线索列）、`note_column`（笔记列）、`summary_column`（总结列）、`mastery`、`capture_id`；**★ SRS 列**：`due_date`、`ease_factor`(默认 250)、`repetitions`、`interval_day`、`lapse_count`、`review_count`、`last_reviewed_at` |
| `wb_review_card` | 模块三（旧复习系统）· 复习卡片 | `front`、`back`、`ease_factor`(默认 250 = EF2.50)、`repetitions`、`interval_day`、`next_review_time`、`lapse_count`、`suspended`。**注**：`/api/workbench/reviews/*` 旧系统专用；新间隔复习系统（§7.12）不复用此表 |
| `wb_review_log` | 复习流水 | `card_id`、`quality`(0~3)、`interval_day`、`ease_factor`、`cost_ms`、`reviewed_at` |
| `wb_palace` | 记忆宫殿 | `name`、`theme`(ROOM/STREET/CAMPUS/CUSTOM)、`cover_color` |
| `wb_palace_loci` | 宫殿位点 | `palace_id`、`name`、`knowledge_point`、`image_hint`、`icon`、`pos_x`/`pos_y`、`sort_order`、`capture_id`/`note_id`/`category_id`；**★ SRS 列**：`mastered_level`(0~5)、`due_date`、`ease_factor`、`repetitions`、`interval_day`、`lapse_count`、`review_count`、`last_reviewed_at` |
| `wb_recall_session` | 主动回忆会话 | `source_text`、`round1~3_text`/`_score`、`current_round`、`status`(IN_PROGRESS/COMPLETED)、`round3_due_time` |
| `wb_story` | 模块四 · 费曼故事 | `audience`、`metaphor`、`content`、`gap_note`、`clarity_score`、`word_count`、`status`(DRAFT/DONE) |
| `wb_embedding` | 向量索引 | `entity_type`(capture/note/story)、`entity_id`、`model`、`dim`、`vector`(JSON 数组)、`content_hash` |

> 索引：`idx_wb_embedding_entity (entity_type, entity_id)`、唯一索引 `idx_wb_embedding_uniq (entity_type, entity_id, model)`。

**时间字段统一用 ISO 8601 字符串**（`created_at` / `updated_at` / `next_review_time` / `due_date`），排序与区间比较直接走字符串比较。

#### ★ SRS 列下沉到源表（v1.1.0 重要架构决策）

新的间隔复习系统（§7.12）**没有**新建卡片表，而是把 SM-2 所需的 7 个字段**直接扩展到源表** `wb_note` 与 `wb_palace_loci` 上：

| 列 | 类型 | 默认值 | 含义 |
|----|------|--------|------|
| `due_date` | TEXT | `'1970-01-01T00:00:00.000Z'` | 下次到期时间。默认值为纪元起点 → 新数据天然"已到期"，立即进入复习队列 |
| `ease_factor` | INTEGER | `250` | 难度系数 ×100（250 = EF 2.50），整数存储避免浮点误差 |
| `repetitions` | INTEGER | `0` | 连续通过次数 |
| `interval_day` | INTEGER | `0` | 当前间隔天数 |
| `lapse_count` | INTEGER | `0` | 遗忘次数 |
| `review_count` | INTEGER | `0` | 累计复习次数 |
| `last_reviewed_at` | TEXT | NULL | 最近一次评分时间 |

**为什么不建新表**：卡片正反面本就是源数据的投影（笔记的线索列→正面、正文+总结→背面；位点名称→正面、知识要点→背面）。若另建卡片表就要做双向同步，源数据一改卡片就失效。下沉到源表后，复习进度天然跟随数据本体，删源即删卡，零同步成本。

**代价**：无法对同一条源数据建多张卡（如一条笔记拆成 3 个问答）。当前产品形态不需要，若未来需要再引入独立卡表。

老库升级由 `db/index.ts` 的幂等 `addColumn()` 自动补齐这些列（`wb_palace_loci` 的 `last_reviewed_at` / `mastered_level` 在更早版本已存在，重复调用安全）。

### 3.3 迁移策略

无 migration 工具，采用两段式幂等策略（`src-api/src/db/index.ts`）：

1. **`CREATE TABLE IF NOT EXISTS`** — 首次运行完整建表
2. **`addColumn(table, col, def)`** — 用 `PRAGMA table_info` 探测列是否存在，缺失才 `ALTER TABLE ADD COLUMN`。旧库升级到新版自动补齐 50+ 个后加字段，重复执行安全。

### 3.4 从 Web 端迁移数据

`POST /api/workbench/import` 消费 Web 端 `GET /api/workbench/export` 产出的 JSON：

- 导入前用 `hasExistingData()` 检查 8 张业务表，**已有数据则拒绝导入**，避免脏数据
- 内部自增 id 经映射表重定向（capture→note→card→story 的关联关系保持）
- `categoryId` 在桌面端无对应体系，统一置空
- 时间字段经 `normIso()` 归一化为 ISO 字符串

---

## 4. 后端接口手册

### 4.1 全局约定

| 项目 | 约定 |
|------|------|
| 监听 | 仅 `127.0.0.1`，默认端口 8787，`EADDRINUSE` 自动 +1（最多试 20 次） |
| 成功响应 | `onSend` 钩子统一包成 `{ code: 200, data: <payload> }`（与 Web 端 `Result<T>` 一致）；已含 `code`+`data` 的不重复包裹 |
| 错误响应 | `{ code, message }`；AI 错误额外带 `aiCode` |
| 空请求体 | 自定义 `application/json` parser，空 body 解析为 `{}`，不报 400 |
| 前端解包 | `api/request.ts` 响应拦截器自动取 `.data`，业务层拿到裸数据 |
| 静态托管 | `@fastify/static` 通配路由 + SPA 回退；带扩展名的资源如实 404（见 §12） |
| 用户 | 全部端点隐式 `userId = 1`，无鉴权 |

**端点总量：111 个**（v1.1.0 原 87 + 6，收集箱 `/api/inbox` 8 个，「5 大体验升级」再增 `/api/inbox/metadata` GET+POST 共 2 个，「间隔复习体验升级」再增 `/api/reviews` snooze / heatmap / forgetting-curve 共 3 个；2026-08-07 番茄钟 `/api/pomodoro` 5 个：record / stats / config(GET+PUT) / today）

| 分组 | 数量 | 前缀 |
|------|------|------|
| 学习工作台（收集箱7 / 笔记5 / 间隔重复9 / 记忆宫殿10 / 主动回忆5 / 费曼故事5 / 总览1 / 迁移1） | 43 | `/api/workbench` |
| AI 能力 | 21 | `/api/ai` |
| 文档库 | 15 | `/api/library` |
| 思维导图 | 5 | `/api/mindmaps` |
| 分类 | 2 | `/api/categories` |
| ★ 间隔复习（新） | 5 | `/api/reviews` |
| ★ 应用配置（新） | 2 | `/api/config` |
| ★ 全局搜索（新） | 1 | `/api/search` |
| ★ 首页统计（新） | 1 | `/api/dashboard` |
| 健康检查 | 1 | `/api/health` |

（根路径 `GET /` 仅在未构建前端时注册，不计入。）

### 4.2 学习工作台端点（前缀 `/api/workbench`）

**收集箱（7）**

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/captures` | 列表，支持 status / starred / keyword / categoryId 过滤 |
| GET | `/captures/:id` | 详情 |
| POST | `/captures` | 新建，返回 id |
| PUT | `/captures/:id` | 更新 |
| DELETE | `/captures/:id` | 删除 |
| PUT | `/captures/:id/status` | 切换状态（INBOX/PROCESSED/ARCHIVED） |
| PUT | `/captures/:id/star` | 切换星标 |

**康奈尔笔记（5）**：`GET /notes`、`GET /notes/:id`、`POST /notes`、`PUT /notes/:id`、`DELETE /notes/:id`

**间隔重复（9）**

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/reviews` | 卡片列表（按 `next_review_time` 升序），支持 categoryId / noteId |
| GET | `/reviews/draw` | **抽卡**：优先取到期卡（窗口含未来 24h），不足则补 `repetitions=0` 的新卡，上限 100 |
| GET | `/reviews/due-count` | 待复习计数 + 3 条卡面样例（供原生通知轮询） |
| POST | `/reviews` | 建卡（EF 初始 250，`next_review_time` = 当前时刻，即刻可复习） |
| PUT | `/reviews/:id` | 编辑卡面 |
| DELETE | `/reviews/:id` | 删除 |
| POST | `/reviews/:id/grade` | **评分**：跑 SM-2，更新卡片 + 写流水，返回 `nextReviewAt` 时间戳 |
| PUT | `/reviews/:id/suspend` | 挂起/恢复（取反） |
| GET | `/reviews/forgetting-curve` | 遗忘曲线：按日聚合复习量/遗忘量/遗忘率/新卡数，`days` 1~365 默认 30 |

**记忆宫殿（10）**：`GET/POST /palaces`、`GET/PUT/DELETE /palaces/:id`、`GET /palaces/:palaceId/loci`、`POST /loci`、`PUT/DELETE /loci/:id`、`GET /palaces/:id/review/due`（返回熟练度 <3 的待复习位点，SRS 简化判定）

**主动回忆（5）**：`GET /recall-sessions`、`GET /recall-sessions/:id`、`POST /recall-sessions`、`POST /recall-sessions/:id/submit`（提交第 N 轮默写并自动计分）、`DELETE /recall-sessions/:id`

**费曼故事（5）**：`GET /stories`、`GET /stories/:id`、`POST /stories`、`PUT /stories/:id`、`DELETE /stories/:id`

**总览（1）**：`GET /overview` → 返回 11 项统计

```jsonc
{
  "captureTotal": 0, "captureInbox": 0, "captureStarred": 0,
  "noteTotal": 0,
  "reviewDue": 0,      // 已到期且未挂起
  "reviewCount": 0,    // 历史复习总次数
  "palaceTotal": 0, "lociTotal": 0,
  "storyTotal": 0, "storyDraft": 0,
  "reviewLast7d": 0    // 近 7 天复习流水条数
}
```

**迁移（1）**：`POST /import`

### 4.3 其它端点

- `GET/POST /api/categories/tree` — 分类树读写（2）
- `GET /api/health` — `{ status, service, bootId, pid, uptimeMs, dataDir, webDir }`，供宿主健康检查与前端断线重连探活（1）
- `/api/library/*` — 文档库 15 个端点（工作区 / 文件树懒加载 / 笔记读写 / 资源服务 / 检索），见 §7.10
- `/api/mindmaps/*` — 思维导图 5 个端点（CRUD，见 §7.11）；AI 生成 `POST /api/ai/generate-mindmap` 挂在 `/api/ai` 下
- `/api/ai/*` — 21 个端点，见 §8
- `/api/pomodoro/*` — 番茄钟 5 个端点（record / stats / config GET+PUT / today），见 §7.16

### 4.4 ★ v1.1.0 新增端点（6 个）+ 间隔复习体验升级（+3）

**间隔复习（5）** — `routes/review.ts`，注册前缀 `/api`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/reviews/due` | 跨 `wb_note` + `wb_palace_loci` 聚合待复习卡。条件 `due_date <= now OR 熟练度 < 3`；合并后按 `due_date` 升序（最紧急在前），**上限 20 条**。VO 含 `repetitions` / `lapseCount`（前端据此渲染新卡 / 复习卡 / 易忘卡徽章） |
| POST | `/api/reviews/submit` | 提交评分。body `{ cardId, sourceType: 'note'\|'loci', rating: 'hard'\|'good'\|'easy'\|'perfect' }`，跑 SM-2 后回写源表，**并写一条 `wb_review_log` 流水**（热力图 / 遗忘曲线的数据源），返回 `{ ok, sourceType, nextDue, masteredLevel, easeFactor, lapsed }` |
| ★ PUT | `/api/reviews/snooze` | 挂起「稍后再背」。body `{ cardId, sourceType }`，仅把 `due_date` 顺延 **24h**，**不动 ease / repetitions / 熟练度**。进程内 `SNOOZE_GUARD` Map 做 **1 小时同卡防重复**，命中返回 `429 { message, retryAfterSec: 3600 }`；卡不存在返回 404 |
| ★ GET | `/api/reviews/heatmap?days=30` | GitHub 风格日活跃热力图。主源 `wb_review_log` 按天聚合，并以 `wb_note` / `wb_palace_loci` 的 `last_reviewed_at` 补充历史（`date\|cardId` 去重防重复计数）。返回 `{ startDate, endDate, days, data: [{ date, count }] }`，日期连续补零 |
| ★ GET | `/api/reviews/forgetting-curve?days=30` | 旧 `/api/workbench/reviews/forgetting-curve` 的 Drizzle 重实现版。按天聚合复习量 / 遗忘量（`quality < 2`）/ 遗忘率 / 新卡数；**流水为空时回退**到源表 `last_reviewed_at` + `review_count`。返回 `{ startDate, endDate, points[], totalReviews, totalLapses, overallLapseRate }` |

> `days` 参数统一钳制在 `1~365`，越界自动收敛，缺省 30。

> **路径易混淆**：`/api/reviews/due`（新，跨表）与 `/api/workbench/reviews/due-count`（旧，`wb_review_card` 表）是**两套独立系统**，前缀不同，不要互相调用。
>
> **rating → SM-2 quality 映射**：`hard→1`、`good→2`、`easy→3`、`perfect→3`。因既有 `sm2.gradeCard` 把 quality 钳制在 0~3（`PASS_QUALITY=2`），**「完美」与「轻松」在排程上完全等价**。若要做 4 档差异化需先扩展 `sm2.ts`，本轮刻意保持算法零改动。

**全局搜索（1）** — `routes/search.ts`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/search?q=关键词` | 跨 `wb_capture` / `wb_note` / `wb_story` 三表 `LIKE` 模糊检索，合并后按 `created_at` 倒序取前 **15** 条 |

统一返回 VO：`{ type: 'capture'\|'note'\|'story', id, title, content(正文前 30 字), path }`。`path` 为前端可直接 `router.push` 的真实路由（`/inbox?highlightId=<id>` / `/workbench/notes/:id` / `/workbench/story/:id`）。

> `wb_note` **没有** `content` 列，正文散落在 `cue_column` / `note_column` / `summary_column` 三列，检索条件与摘要拼接都需覆盖这三列。
>
> **扩展搜索范围**（如纳入记忆宫殿、文档库）：在同一路由内追加一张表的查询并入合并数组即可，**前端零改动**。

**首页统计（1）** — `routes/dashboard.ts`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/dashboard/stats` | 工作台首页顶部「学习闭环四步」数字气泡 + 「今日聚焦」四卡的唯一数据源 |

```jsonc
{
  "todayCaptures": 0,   // 今日新增灵感（本机时区当日 0 点起）
  "pendingCaptures": 0, // 待整理碎片（status = INBOX）
  "dueReviews": 0,      // 待复习（notes + loci 中 due_date <= now，与 /review 同源）
  "palaceLoci": 0,      // 记忆宫殿总位点数
  "storyDrafts": 0,     // 故事草稿（status = DRAFT）
  "weeklyFlow": 0,      // 近 7 天由 capture 流转生成的笔记数
  "loopSteps": { "step1Count": 0, "step2Count": 0, "step3Count": 0, "step4Count": 0 }
}
```

> `step3Count === dueReviews`，刻意保证「数字↔入口」一致：点气泡跳到 `/review` 后看到的卡片数与气泡数字相符。
>
> **与 `/api/workbench/overview` 的分工**：`overview` 字段更丰富（`captureStarred` / `reviewLast7d` / `lociTotal` 等），专供首页**6 指标「学习数据看板」**；`dashboard/stats` 专供**闭环四步气泡 + 今日聚焦四卡**。两者并存，勿混用。新增统计字段在 `dashboard.ts` 的 `count(tbl, extra[])` 加一行即可。

**应用配置（2）** — `routes/config.ts`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/config` | 应用启动时读取，返回 `{ hasOnboarded, dataDir, ai: publicConfig() }`（API Key 仅掩码回显） |
| POST | `/api/config/init` | 引导完成 / 设置中心保存。body `{ dataDir?, aiSettings?: { apiUrl, apiKey, model }, hasOnboarded? }` |

> `aiSettings.apiKey` **留空表示保持已存 Key 不变**，本端点永不回显明文 Key。AI 参数经 `llm.saveConfig()` 落 `ai-config.json`（600 权限），`hasOnboarded` / `dataDir` 落 `config.json`。
>
> 当前 `dataDir` 仅作为**用户偏好如实记录并回显**：真实数据目录在进程启动那一刻就由 `KNOWFLOW_DATA_DIR` 确定、SQLite 已打开，切换目录需迁移既有数据 + 重启侧车，属后续增强项。

### 4.5 ★ 收集箱 `/api/inbox`（10 个，本次新增）

复用 `wb_capture` 表（**未新建并行表**），由 `routes/inbox.ts` 注册于前缀 `/api`。状态机：DB 大写 `INBOX / ARCHIVED / TRASHED` ↔ 前端 VO 小写 `unprocessed / archived / trashed`（`inbox.ts` 的 `toStatusVo` / `toStatusDb` 集中映射）。

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/inbox/list` | 收集箱列表；支持 `?status=unprocessed\|archived\|trashed` 与 `?q=` 关键词过滤，`?sort=asc` 按创建时间升序（收件箱积压视图用），默认倒序 |
| GET | `/api/inbox` | 列表别名（同上，便于兼容不同前端调用） |
| POST | `/api/inbox` | 新建卡片。body `{ type: 'text'\|'link'\|'image', title?, content, sourceUrl?, tags[] }`；标题缺省时从正文首行或来源域名自动生成 |
| GET | `/api/inbox/clip?url=` | 网页剪藏：真实抓取（axios + cheerio + iconv-lite，10s 超时 / UA 伪装 / GBK 解码 / 5MB 上限 / 剥离 script·style·nav·footer 取 120 字摘要），失败优雅降级返回 `{ ok:false }` |
| POST | `/api/inbox/clip` | 同上，body `{ url }` |
| GET | `/api/inbox/metadata?url=` | 网页元数据爬取（「5 大体验升级」B）：抓 `og:title`/`og:description`/`og:image`/`<title>` + 前 200 字摘要；失败**仍返回 200** 且 `ok:false`，绝不阻塞用户手动录入 |
| POST | `/api/inbox/metadata` | 同上，body `{ url }` |
| PUT | `/api/inbox/:id` | 改标题 / 正文 / 标签 / 来源 |
| PUT | `/api/inbox/:id/process` | 一键沉淀（「5 大体验升级」C 多目标流转）。`{ target: 'note' }`→写入文档库 `.md`（未选 vault 时 409）；`{ target: 'cornell' }`→插入 `wb_note` 带 `captureId` 回链；`{ target: 'palace', palaceId, lociId? }`→新建或挂接待选 `wbPalaceLoci` 位点并回链 `captureId`；`{ target: 'story' }`→生成费曼故事草稿（`wb_story`）带 `captureId` 回链；成功后本卡片归档 |
| DELETE | `/api/inbox/:id` | 软删除（置 `TRASHED`，非物理删） |

> **一键沉淀落点**：`note` 走 `createNote` / `writeNote`（与文档库同源），`cornell` 走 `wb_note` 插入（与康奈尔笔记同源），`palace` 走 `wbPalaceLoci`（与记忆宫殿同源），`story` 走 `wb_story`（与费曼故事同源），均不破坏既有数据表，且都带回 `captureId` 溯源。

> **收件箱归零提醒（「5 大体验升级」D）**：`GET /api/dashboard/stats` 在既有字段外新增 `inboxOverdueCount`，统计 `wb_capture` 中 `status=INBOX` 且 `created_at` 早于 `now-72h` 的条数；首页「今日聚焦」据此展示橙色积压卡片或「闭环」完成态。

> **前端状态（「5 大体验升级」E）**：`stores/inboxStore.ts`（`useInboxStore`）统一持有列表/加载/速记开关/筛选等状态，`process` 支持多目标；经 `pinia-plugin-persistedstate` 仅持久化 `filters`（视图偏好），列表等运行时状态不入盘。全局速记面板由 `App.vue` 监听 `Cmd/Ctrl+Shift+I` 驱动 `quickOpen` 开关（`components/QuickCaptureModal.vue`）。

---

## 5. 核心算法

### 5.1 SM-2 间隔重复（`services/sm2.ts`）

精确平移自 Web 端 `WorkbenchServiceImpl.gradeReview`，确保跨端排程不漂移。

**评分区间 0~3**（非经典 SM-2 的 0~5），`quality >= 2` 视为通过：

```
EF' = EF + (0.1 - (5-q) × (0.08 + (5-q) × 0.02))     下限 1.30
```

> 注意：EF 以**整数百分数**存储（250 表示 2.50），避免浮点误差。

**间隔推进**：

| 情形 | repetitions | interval |
|------|------------|----------|
| 遗忘（q < 2） | 归零 | 1 天，`lapse_count + 1` |
| 首次通过 | 1 | 1 天 |
| 第二次通过 | 2 | 6 天 |
| 第三次及以后 | +1 | `round(上次间隔 × EF)` |

评分后写 `wb_review_log` 一条流水，`next_review_time = now + interval × 86400000`。

### 5.2 主动回忆计分（`routes/recall.ts`）

**三轮闭卷默写**流程：

```
第1轮默写 → 自动计分 → currentRound=2
第2轮默写 → 自动计分 → currentRound=3，round3_due_time = now + 1 小时（强制间隔）
第3轮默写 → 自动计分 → status=COMPLETED，写 completed_time
```

**规则法计分**（0~100）：

1. 分词：英文按单词（长度 ≥2），中文按单字，全部小写去重成 Set
2. 命中率 = `|默写词 ∩ 原文词| / |原文词|`，× 100 取整

返回 VO 附带 `scoreTrend: [r1, r2, r3]` 与 `improvementPct`（相邻轮次进步百分比，前值为 0 时按 0/100 处理）。

> 该规则法只能衡量**字面命中**，无法识别同义表达，这也是 AI 语义评分（§8）存在的原因——AI 评分会同时返回规则分作为对照锚点。

### 5.3 向量相似度（`routes/ai.ts`）

- 向量以 JSON 数组存 `wb_embedding.vector`（better-sqlite3 无原生 vector 类型）
- 相似度在**应用层 JS** 计算余弦值，显式归一化以兼容维度不一致的情形
- `content_hash` 为轻量 djb2 指纹（`长度:hash36`），内容未变则跳过重算，避免重复调用向量服务
- 待向量化文本 = 标题 + 正文，`stripHtml` 后截断 6000 字符

---

## 6. 前端架构

### 6.1 路由表（`src-ui/src/router/index.ts`）

history 模式，业务路由 `meta: { layout: 'c', fullscreen: true }`；桌面端单用户，**去掉了 Web 端的 `requiresAuth` 守卫**，但新增了**首次引导门禁守卫**。共 **24 条配置 / 20 个业务视图**（新增 `/inbox` 路由、`/workbench/capture`→`/inbox` 重定向，复习模块 2026-08-07 收敛新增 `/workbench/review/card-list`（传统卡组）与 `/review/flashcard`（闪卡专注模式别名），2026-08-07 番茄钟新增 `/pomodoro` 与 `/pomodoro/stats`）。

| 路径 | 名称 | 视图 | 备注 |
|------|------|------|------|
| `/` | — | 重定向 → `/workbench` | |
| ★ `/onboarding` | Onboarding | 新手引导 | `meta.standalone`，无顶栏全屏 |
| ★ `/settings` | Settings | 全局设置中心 | `meta.standalone`，无顶栏全屏 |
| `/workbench` | Workbench | 工作台总览（首页） | |
| `/inbox` | Inbox | 收集箱（极速输入 → 沉淀） | `meta.{layout:'c',fullscreen:true}` |
| `/workbench/capture` | — | 重定向 → `/inbox`（旧路径兼容） | |
| `/workbench/notes` | WorkbenchNotes | 笔记列表 | |
| `/workbench/notes/:id` | WorkbenchNoteEdit | 笔记编辑（康奈尔三栏） | |
| `/workbench/review` | WorkbenchReview | 复习驾驶舱（双系统总入口：看板 + 分流） | 2026-08-07 合并新旧复习入口，见 §7.12 |
| `/workbench/review/card-list` | WorkbenchReviewCards | 传统复习 · 自定义卡组（**旧系统** `wb_review_card`） | 从驾驶舱入口卡片下沉 |
| ★ `/review` | Review | 间隔复习（**新系统**，跨表 SRS 翻卡） | 见 §7.12 |
| `/review/flashcard` | ReviewFlashcard | 闪卡专注模式（与 `/review` 共用 Review 视图） | 深链别名 |
| `/workbench/palace` | WorkbenchPalace | 记忆宫殿列表 | |
| `/workbench/palace/:id` | WorkbenchPalaceEdit | 宫殿编辑（位点布局） | |
| `/workbench/recall` | WorkbenchRecall | 主动回忆 | |
| `/workbench/story` | WorkbenchStory | 费曼故事列表 | |
| `/workbench/story/:id` | WorkbenchStoryEdit | 故事编辑 | |
| `/settings/ai` | AiSettings | AI 配置中心 | |
| `/insights/ai` | AiInsights | AI 学习洞察 | |
| `/library` | DocLibrary | 文档库（Obsidian 式本地 Markdown 工作台） | |
| `/mindmap` | MindMap | 思维导图（大纲 / markmap / vue-flow） | |
| `/:pathMatch(.*)*` | — | 重定向 → `/workbench` | |

全部视图**懒加载**（`() => import(...)`），`scrollBehavior` 统一回到顶部。

#### ★ 引导门禁守卫（`router.beforeEach`）

```
① 首次进入前 await store.initFromBackend()  与后端 /api/config 对齐一次（后端为权威源，
                                             覆盖 localStorage 水合值，处理"本地缓存被清
                                             但后端已标记完成"的边界）
② 未引导 且 目标 ≠ /onboarding   → 强制跳 /onboarding
③ 已引导 且 目标 = /onboarding   → 回 /workbench，避免重复引导
   例外：?rerun=1 放行（设置中心「重新运行新手引导」带出）
```

#### ★ `meta.standalone` 全屏独立页约定

`/onboarding` 与 `/settings` 标记 `meta: { standalone: true }`，`App.vue` 据此**隐藏 `DesktopTopNav`** 且内容区不套 `max-w-7xl` 居中框。新做无顶栏的聚焦页（引导 / 设置类）沿用此约定。

> **路径规划红线**：新页面路径**不要挂在 `/workbench` 前缀下**。顶栏 `isActive` 用 `startsWith` 判定（仅 `/workbench` 本身特判精确匹配），挂在该前缀下会与同级导航项互相误高亮。这也是新复习系统用 `/review` 而非 `/workbench/review2` 的原因。
> **例外**：`/workbench/review/card-list`（传统卡组）特意挂在 `/workbench/review` 下——它与驾驶舱同属「复习」上下文；顶栏「复习」项的 `isActive` 用 `match` 数组把 `/workbench/review`、`/review`、`/review/flashcard` 一并高亮，故不会出现误高亮。

### 6.2 布局与全局组件

- **`App.vue`** — 顶部固定 56px 的 `DesktopTopNav` + `pt-14` 内容区；`standalone` 路由隐藏顶栏、`fullscreen` 路由不做居中约束；根部挂 `ToastHost` / ★`CommandPalette` / ★`ConnectionOverlay`；★`onMounted` 调 `initBackendHealth()` 建立 bootId 基线；监听 `window` keydown 实现 `Cmd/Ctrl+K` 开合命令面板、`Esc` 关闭
- **`DesktopTopNav.vue`** — **9 个主导航**（工作台/收集箱/笔记/文档库/复习/记忆宫殿/主动回忆/费曼故事/思维导图）；2026-08-07 复习模块收敛后「间隔复习」项已并入「复习」，顶栏 `isActive` 用 `match` 数组把 `/review`、`/review/flashcard`、`/workbench/review` 全部高亮，窗口窄于 lg 时折叠成下拉；右侧「本地离线」徽章 + ★「搜索 ⌘K」按钮（复用 `openPalette()`）+ AI 设置入口（配置就绪时高亮）+ 检查更新按钮（调 Tauri `check_for_update` 命令）。`navItems` 是集中式数组，新增页面在此登记
- **★ `CommandPalette.vue`** — 全局命令面板，见 §7.13
- **★ `ReviewHeatmap.vue`** — 复习热力图（7×7 GitHub 风格日活跃网格），见 §7.12
- **★ `ui/ConnectionOverlay.vue`** — 后端断连全局遮罩，见 §6.6
- **`ui/Icon.vue`** — 统一图标组件，**lucide-vue-next 包装器**（传入 `map-pin`/`brain`/`route` 等合法 lucide 名即渲染）；尺寸走 `--kb-icon-*` 梯度；未命中时回退到内置 220+ 手写 SVG 兜底
- **`ui/ToastHost.vue`** — 全局提示宿主，支持 success/error/warning/info 四类（左侧色边 + 图标），并承载 confirm / prompt 弹窗
- **`AiAssociatePanel.vue`** — 内容关联侧面板（重建索引 / 查找关联，展示余弦相似度）

> **`Icon.vue` 渲染分支必须是严格 5 路互斥链**：`<img v-if="isImageIcon">` → `<i v-else-if="isIconfontIcon">` → `<div v-else-if="isSvgCodeIcon">` → `<component v-else-if="lucideComponent">` → `<svg v-else>`（手写兜底）。若把 lucide 块写成第一个 `v-if`，兜底 `<svg v-else>` 会因抢链失败而同时渲染，导致**同一图标画两份**。

### 6.3 设计 token 体系

`src-ui/src/style.css`（1427 行）是**唯一 token 来源**，所有样式改动必须收敛到这里：

| token 族 | 作用 |
|---------|------|
| 色彩 | 主色、语义色（成功/警告/危险/信息）、中性色阶、文本四级 |
| 圆角 | `--kb-radius-*` 梯度 |
| 阴影 | `--kb-shadow-*` |
| 字号 | `--kb-fs-*` 阶梯 |
| 图标 | `--kb-icon-*` 尺寸梯度 |
| 间距 | `--kb-space-*` |
| 导航 | 高度、内边距等语义尺寸 |

补充说明：

- `tailwind.config.js` 中的 `primary` 等色板须与 `--kb-*` 保持一致，不得各自定义
- 支持 `data-theme='dark'` 深色模式
- 各业务模块保留自己的 `themeColor` 品牌语义色（刻意不统一，用于区分模块）
- 共享样式：`views/workbench-shared.css`（`.wb-icon-btn`、`.wb-empty`、`.wb-skeleton` 等）、`views/ai-shared.css`

### 6.4 请求层

```
api/request.ts   单例 axios：baseURL '/api'，timeout 15000ms
                 响应拦截器解包 ApiResult<T>，code≠200 时抛出业务错误
                 ★ onRejected 承载断线重连与请求重放（见 §6.6）
api/workbench.ts 学习工作台端点封装
api/ai.ts        AI 端点封装，超时放宽到 90000ms（embedding 同步 120000ms）
api/review.ts    ★ 间隔复习（/api/reviews due、submit、snooze、heatmap、forgetting-curve）
api/search.ts    ★ 全局搜索（/api/search）
api/dashboard.ts ★ 首页统计（/api/dashboard/stats）
api/config.ts    ★ 应用配置（/api/config、/api/config/init）
api/types.ts     全量 TS 类型定义
```

**三态处理约定**：

- 加载中 → `wb-skeleton` / `animate-pulse` 骨架屏
- 空数据 → `wb-empty` 空态组件
- 出错 → `notify(getApiError(e), 'error')` 统一 toast

### 6.5 构建配置

`vite.config.ts`：别名 `@` → `src`；dev 代理 `/api` → `http://127.0.0.1:8787`；产物输出 `dist`。

### 6.6 ★ 断线自动重连（v1.1.0）

宿主侧 `SidecarManager`（§9.4）会自动重启崩溃的侧车，但重启窗口期（约 2~6 秒）内前端所有请求都会 `ECONNREFUSED`。若不处理，用户看到的就是满屏报错 + 需要手动刷新。本机制让这段窗口对业务层**完全无感**。

**核心文件**：`src-ui/src/utils/connection.ts`（连接状态机）+ `src-ui/src/api/request.ts`（拦截器接入）+ `src-ui/src/components/ui/ConnectionOverlay.vue`（遮罩 UI）

```
业务请求失败
   │
   ├─ 有 response（后端答复了 4xx/5xx）→ 业务错误，原样抛出，不触发重连
   │
   └─ 无 response + 网络类错误（ERR_NETWORK / ECONNREFUSED / ECONNRESET / EPIPE）
         或超时（ECONNABORTED / ETIMEDOUT）→ 且重放次数 < 3
            │
            ├─ 超时先探一次 /api/health：探得通说明后端还在（只是这个请求慢）→ 不重连，抛超时
            │
            └─ ensureBackendAlive()  全局单例重连任务
                  │  并发失败请求共享同一个 Promise，绝不产生探活风暴
                  ├─ 挂遮罩「知识引擎断连，正在尝试重连…」
                  ├─ 指数退避轮询 /api/health：500 → 1000 → 2000 → 4000 → 8000ms（封顶）
                  │   探活走独立裸 axios 实例（timeout 3000ms），避免拦截器自我递归
                  │   校验响应体 service 字段，确认是自家后端
                  ├─ attempt ≥ 4 时遮罩显示「立即重试」/「重启知识引擎」按钮
                  │   （后者调 Tauri `restart_sidecar` 命令）
                  └─ 恢复 → 隐藏遮罩 + 成功 toast → replayRequest(config) 重放原请求
```

**不设失败上限**：宿主侧也是无限重启策略，前端先放弃只会让用户对着死界面。用户想干预可点遮罩上的按钮。

**bootId 基线**：`App.vue` 在 `onMounted` 调用 `initBackendHealth()` 记录初始 `bootId`。后续恢复时若 `bootId` 变化，说明**后端换了新实例**（而非网络抖动），置 `backendRestarted` 标志，UI 可据此提示用户"引擎已重启"。没有这个基线，侧车**第一次**被重启时会漏判。

> **⚠️ 重放必须用公共方法，不能用 `request.request(error.config)`**
> axios 的派发流程会改写 `error.config`（baseURL 合并、url 可能被写成绝对地址）。把这份 config 原样交回 `request.request()` 重放，会触发「绝对地址被当成请求行原样发出 / URL 前缀翻倍」，重放请求落到 404、业务层永久卡死。
> 正确做法是 `replayRequest(config)`：只取 `method` / `url` / `data` / `params` / `headers`，重新走 `request.get/post/put/patch/delete` 公共方法派发，URL 组合逻辑与首发请求逐字节一致。

**验证**：已用真实 HTTP 服务做端到端集成测试（中途真 kill 再拉起），12/12 断言通过——正常请求、断线进入 `reconnecting`、在途请求自动重放成功、恢复 `online`、bootId 变化识别、尝试次数归零、并发请求共享单轮轮询、业务错误原样抛出不触发重连。

---

## 7. 功能手册（按模块）

### 7.1 工作台总览 `/workbench`（v1.1.0 已动态化）

学习闭环的入口首页。**并存两个聚合数据源，各司其职、勿混用**：

| 区块 | 数据源 | 说明 |
|------|--------|------|
| 顶部「学习闭环四步」数字气泡 + 「今日聚焦」四卡 | ★ `GET /api/dashboard/stats` | v1.1.0 新增，把原先的静态占位数字全部替换为真实统计 |
| 「学习数据看板」6 指标 | `GET /api/workbench/overview` | 字段更丰富（`captureStarred` / `reviewLast7d` / `lociTotal` 等） |

**今日聚焦四卡入口**：待整理碎片 → `/inbox`；**待复习 → `/review`（新复习系统，不是旧的 `/workbench/review`）**；宫殿位点 → `/workbench/palace`；故事草稿 → `/workbench/story`。

前端状态走 `store/dashboardStore.ts`（Pinia setup store，空值兜底，`fetchStats` 失败静默不打断页面）。

> **⚠️ 命名冲突陷阱**：`Workbench.vue` 里用 `storeToRefs` 取 store 时**必须把 `stats` 别名成 `dashboard`**——组件内已存在一个叫 `stats` 的 computed（6 指标看板）和一个叫 `loading` 的 ref，直接解构会撞名。

无 AI 能力。

### 7.2 收集箱 `/inbox`（原 `/workbench/capture`，已 301 重定向）

**定位**：信息入口，把任何碎片先无压力地扔进来，后续再加工。

用户可以：

- 新建 / 编辑 / 删除收集卡片（标题 + 正文 + 来源类型 `text\|link\|image` + 来源链接 + 标签）
- 按状态筛选（收件箱 `unprocessed` / 已归档 `archived` / 已删除 `trashed`）、按关键词搜索
- 极速输入：粘贴 URL 自动触发**网页剪藏**预览（axios + cheerio + iconv-lite 真实抓取），`Ctrl/⌘+Enter` 提交；全局 `Ctrl/⌘+Shift+I` 唤起收集弹窗
- 一键沉淀：🚀 下拉可将卡片推进为**康奈尔笔记**（`wb_note`，带 `captureId` 回链）或**文档库 Markdown**（vault `.md`），沉淀后自动归档

**AI 增强**：

| 按钮 | 端点 | 作用 |
|------|------|------|
| 提炼总结 | `POST /api/ai/capture/summarize` | 生成摘要与要点 |
| 建议标签 | `POST /api/ai/tags` | 推荐标签（可多选回填） |
| 起草笔记 | `POST /api/ai/capture/draft-note` | 直接生成康奈尔三栏草稿 |

### 7.3 康奈尔笔记 `/workbench/notes` + `/workbench/notes/:id`

**定位**：结构化加工。严格遵循康奈尔笔记法的三栏结构。

| 栏位 | 字段 | 作用 |
|------|------|------|
| 线索列 | `cue_column` | 关键词、问题、提示 |
| 笔记列 | `note_column` | 正文详情 |
| 总结列 | `summary_column` | 一段话总结 |

用户可以：

- 列表页：搜索、网格卡片 / 紧凑列表双模式切换、掌握度进度条、`due_date` 到期/过期打「需复习」徽章（新笔记打「待首复习」）；进入编辑、删除
- 编辑页：**倒 T 形三栏可拖拽调节比例**（竖分割线调线索栏宽、横分割线调总结栏高，比例持久化到 `useNoteStore` 并双击分割线复位）+ **自动保存**（串行化 `saveChain` 防竞态、`onBeforeRouteLeave` 强存）+ **划词悬浮工具栏**（选中文本释放→Bold / Highlighter / 转线索，转线索写入线索栏末尾），维护标签（兼容 JSON 数组与逗号分隔双格式）与掌握度（`mastery`）
- **极速新建**：全局 ⌘/Ctrl+Shift+F 唤起 `QuickCreateNote` 弹窗，填标题 + 笔记列即创建并跳转 `/workbench/notes/:id`
- **导出**：全应用唯一的导出实现点
  - `exportImage()` — html2canvas 导出 PNG
  - `exportPDF()` — html2canvas + jsPDF 生成 **A4 多页** PDF

**AI 增强**：

| 按钮 | 端点 | 作用 |
|------|------|------|
| 生成线索列 | `POST /api/ai/note/generate` | 由笔记列反推线索与总结 |
| 生成复习卡 | `POST /api/ai/note/flashcards` | 批量产出问答卡，**自动入库 `wb_review_card`**（`next_review_time=now` 立即进复习队列） |

### 7.4 间隔重复复习 `/workbench/review`

**定位**：长期记忆的调度器，基于 SM-2。

用户可以：

- **抽卡复习**：`GET /reviews/draw` 取到期卡（含未来 24h 窗口），不足时补新卡
- **四档评分**：quality 0~3（0 完全忘记 / 1 有印象 / 2 想起来了 / 3 秒答），提交后即时看到下次复习时间
- 新建、编辑、删除卡片
- 挂起 / 恢复卡片（暂时不参与排程）
- 复习统计（热力图 / 遗忘曲线）统一在**复习驾驶舱**（`/workbench/review`）查看，本页只做刷卡与管卡

无 AI 按钮（AI 复习推荐在洞察页）。

### 7.5 记忆宫殿 `/workbench/palace` + `/workbench/palace/:id`

**定位**：空间记忆法，把知识点挂到虚拟空间的「位点」上。

用户可以：

- 创建宫殿：名称、描述、主题（房间/街道/校园/自定义）、封面色
- 进入宫殿编辑：新增位点，填写位点名、绑定知识点、图像联想提示（`image_hint`）、图标
- **拖拽布局**：位点在画布上按 `pos_x` / `pos_y` 自由摆放，并可调整 `sort_order` 游览顺序
- 删除宫殿或位点

**AI 增强**：`POST /api/ai/palace/loci` — 根据知识点批量生成位点建议（含图像联想提示）。

### 7.6 主动回忆 `/workbench/recall`

**定位**：三轮闭卷默写，检验真实掌握程度。

用户可以：

- 从笔记或自定义文本创建回忆会话（`source_text` 为标准答案）
- 闭卷默写并逐轮提交，系统**自动计分**（见 §5.2）
- 查看三轮分数趋势与相邻轮次进步百分比
- 第 2 轮提交后系统设置 **1 小时冷却**（`round3_due_time`），到点再做第 3 轮
- 第 3 轮提交后会话标记完成

**AI 增强**：

| 按钮 | 端点 | 作用 |
|------|------|------|
| 语义评分 | `POST /api/ai/recall/score` | 用 LLM 做语义级评分，弥补字面计分的短板，同时返回规则分对照 |
| 改进建议 | `POST /api/ai/recall/advice` | 指出遗漏点与下一步复习建议 |

### 7.7 费曼故事 `/workbench/story` + `/workbench/story/:id`

**定位**：输出验证。用大白话把知识讲给特定对象听，讲不清即未掌握。

用户可以：

- 新建故事：标题、**目标听众**（`audience`）、**核心比喻**（`metaphor`）、正文
- 记录 **理解缺口**（`gap_note`）——讲述过程中卡壳的地方
- 维护状态（草稿 / 完成）、字数统计
- 列表浏览与删除

**AI 增强**：

| 按钮 | 端点 | 作用 |
|------|------|------|
| 清晰度评分 | `POST /api/ai/story/clarity` | 给出 0~100 清晰度分与改进建议 |
| 从笔记起草 | `POST /api/ai/story/draft` | 由指定笔记生成费曼式初稿 |

### 7.8 AI 配置中心 `/settings/ai`

- 选择服务商预设（DeepSeek / OpenAI / 自定义 OpenAI 兼容网关），一键填充 baseUrl 与模型
- 填写 API Key、模型名、温度（0~2）、超时（5s~180s）
- 单独配置**向量化服务**（SiliconFlow BAAI/bge-m3 / OpenAI text-embedding-3-small / 自定义）
- **连通性测试**（`POST /api/ai/test`）与能力状态查询（`GET /api/ai/status`）
- 页面同时是 AI 能力清单的导航入口（可进入洞察页）

### 7.9 AI 学习洞察 `/insights/ai`

- **学习周报**（`POST /api/ai/insight/report`）：基于近期数据生成总结
- **薄弱点诊断**（`POST /api/ai/weakness/diagnose`）：定位掌握薄弱的主题
- **复习推荐**（`POST /api/ai/review/recommend`）：给出优先复习清单
- **内容关联**（右侧 `AiAssociatePanel`）：先「重建索引」（`POST /api/ai/embeddings/sync`）把收集/笔记/故事向量化，再「查找关联」（`POST /api/ai/associate`）按余弦相似度列出相关内容，可点击跳转对应页面

---

### 7.10 文档库 `/library`（Obsidian 式本地 Markdown 工作台）

把任意本地文件夹当作 vault 来管理 Markdown 笔记，强调「本地文件即数据」——不导入数据库，直接读写原文件。

**核心能力**

- **三栏工作区**：左=目录树（懒加载，展开才请求子层；搜索走后端全库检索）、中=编辑/分栏/预览三模式、右=大纲（TOC，显隐偏好持久化到 localStorage）。
- **自动保存**：⌘S 手动保存 + 600ms 防抖自动保存（含 `savedSnapshot` 比对防「载入即触发」、`activeNoteId` 入队锁防「防抖期切文件写错文件」、切文件/离开/卸载/beforeunload 冲刷）。
- **Obsidian 语法兼容**：双链 `[[笔记]]` / `[[笔记#标题]]` / `[[笔记|别名]]`、嵌入块 `![[笔记]]` / `![[笔记#标题]]`、图片嵌入 `![[图片]]` / `![[图片|WxH]]`；标准 `![]()` 本地图片改写为同源资源 URL。
- **双链导航**：点击 `[[...]]` 跳转到对应笔记/资源；解析器兼容「省略 `.md` 扩展名」与「绝对路径（从库根）/相对路径（`./`、`../`）」多种写法，与 Obsidian 行为对齐。
- **资源服务**：图片/SVG/PDF 经 `/api/library/asset` 同源流式返回（`safeResolve` 防越界 + MIME 白名单 + `nosniff` + 缓存）；仅给文件名的 `![[图片]]` 经 `findAsset` 按全库 basename 纠正路径。
- **目录树折叠**：左栏可折叠/展开（`.dl-shell.is-collapsed`），收起后内容区自动占满，布局平滑协调；偏好持久化。

**后端（前缀 `/api/library`，15 端点）**：`GET/POST /workspace(/init)`、`GET /fs/browse`、`GET /notes/tree`、`GET /notes/children?parent=`、`GET /notes/all`、`GET /notes/search?q=`、`GET /notes/content`、`POST /notes/create`、`POST /notes/rename`、`POST /folders/create`、`PUT /notes/update`、`DELETE /notes/delete`、`GET /asset?path=`、`GET /asset/find?name=`。

**关键设计取舍**

- 文件树**懒加载**而非一次建全树：超大库（实测 1.4 万目录 + 6000+ md）若全量建树会截断且 DOM 卡死；改为「根层 + 展开才拉子层」，双链索引改用 `GET /notes/all` 全量扁平清单解耦（深层双链也能命中）。
- markdown-it 开 `html:false` 时，内置 `html_inline` 会被吞、程序 `push('image')` 的 token `children` 为 null 会崩；改为注册自定义 `obsidian_embed` token + 自定义渲染器安全回显（属性值自行转义）。
- 全局错误兜底只对「启动期脚本失败」弹遮罩，捕获阶段的 `<img>` 404 等**资源错误只记日志**，不再把工作台误判为「界面启动失败」。

### 7.11 思维导图 `/mindmap`

同一份文档支持三种视图形态，互相联动。

**三视图**

- **极简大纲**（contenteditable）：Tab / Shift+Tab 升降级、Enter 新建、⌥⇧↑↓ 跨级移动、拖拽防祖先环；用 `v-mm-text` 指令避免光标跳动，并处理 IME `isComposing`。
- **markmap 导图**：`markmap-lib` transform → `markmap-view` 渲染，CSS 变量主题注入，支持 SVG 导出（导出时把 `var()` 替换为具体值）。
- **vue-flow 流程图**：`@vue-flow/core` + background + controls，5 种节点形状、双击编辑、Delete 删除。

**AI 生成**：`POST /api/mindmaps/:id/generate-mindmap`（复用 `/api/ai` 内 `buildMindMapPrompt`，强约束纯 JSON / MECE / 深度 2~5 / 分支 3~8）；未配 Key 时返回 mock，整体优雅降级。

**后端（前缀 `/api/mindmaps`，5 端点）**：`GET /`、`POST /`、`GET /:id`、`PUT /:id`、`DELETE /:id`。数据以 fs JSON 持久化到 `<dataDir>/mindmaps/<id>.json`（tmp + rename 原子写），`seedIfEmpty()` 首次预置「我的学习路线」（31 节点、≥3 级深）。

AI 生成端点 `POST /generate-mindmap` **单独挂载在 `/api/ai` 下**（由 `mindmapAiRoutes` 注册，见 §8.3），复用 `/api/ai` 内的 `buildMindMapPrompt`，未配 Key 时返回 mock 优雅降级。

---

### 7.12 ★ 间隔复习 `/review`（v1.1.0 新增）

**定位**：把散落在「康奈尔笔记」与「记忆宫殿位点」里的知识点，统一收拢成一条 SM-2 排程的翻卡复习流。

与旧的 `/workbench/review`（模块三，基于 `wb_review_card` 手动建卡）的区别：

| 维度 | 旧系统 `/workbench/review` | ★ 新系统 `/review` |
|------|---------------------------|-------------------|
| 卡片来源 | `wb_review_card` 表，**用户手动建卡** | 自动来自 `wb_note` + `wb_palace_loci`，**零建卡成本** |
| 端点 | `/api/workbench/reviews/*`（9 个） | `/api/reviews/due`、`/submit`、`/snooze`、`/heatmap`、`/forgetting-curve`（5 个） |
| SRS 数据位置 | 独立卡表 | **下沉到源表**（见 §3.2） |
| 复习流水 | 写 `wb_review_log` | **也写 `wb_review_log`**（submit 时补记），支撑热力图 / 遗忘曲线 |
| 定位 | 精细化卡片管理（可编辑卡面、挂起、遗忘曲线） | 轻量日常刷卡 + 动效反馈 + 挂起 + 全屏专注 + 统计 |

两套系统**刻意并存**、互不影响。2026-08-07 顶栏「间隔复习」项已并入「复习」，用户统一从**复习驾驶舱**（`/workbench/review`）分流：看板区展示双系统待复习数与记忆健康度（热力图 + 遗忘曲线），入口卡片分别导向新系统闪卡（`/review`）与传统卡组（`/workbench/review/card-list`）。

**交互流程**

```
进页 → GET /api/reviews/due（最多 20 张，按 due_date 升序）
     → FlashCard 展示正面（笔记线索列 / 位点名称）+ 右上角卡型徽章
     → 点击翻转（CSS 3D transform）看背面（笔记正文+总结 / 位点知识要点）
     → 四档评分：困难 / 一般 / 轻松 / 完美（键盘 1/2/3/4）
       ├ 卡片按方向旋转飞出（困难←左，其余→右，CSS @keyframes fc-fly-out）
       ├ 屏幕中央弹反馈短语（🤔 记住它！/ ✅ 完美！…），500ms 自动淡出
       └ animationend 后才 POST /api/reviews/submit → SM-2 回写源表 + 写 wb_review_log
     → queue.splice(0, 1)，下一张淡入（currentIndex 恒为 0）
或   → 「⏸️ 稍后再背」（键盘 0）→ PUT /api/reviews/snooze → due_date +24h，同样飞出跳过
```

**四大体验升级（本轮）**

| 能力 | 实现要点 |
|------|---------|
| 卡片飞出 + 反馈动画 | `FlashCard` 接 `fly={ active, dir }` prop 驱动 `.fc-fly-left/right`；只在 `animationName === 'fc-fly-out'` 时 emit `flyEnd`，避免 3D 翻转动画误触发。父级在 `flyEnd` 里先把卡槽 `is-swapping` 隐身、再复位动画类、再 await 接口（内部 splice），最后淡入下一张——**旧卡不会回弹一帧**，也不干涉 `currentIndex === 0` 的队列约定 |
| 进度与卡型可视化 | 进度分母口径改为 `processedCount = totalCount - queue.length`（评分与挂起都推进）；卡片右上角按 `repetitions` / `lapseCount` 渲徽章：`lapseCount > 2 → ⚠️ 易忘卡`（红）> `repetitions === 0 → 💡 新卡`（绿）> `🔄 复习卡`（橙） |
| 挂起 Snooze | 前端 `snoozeCard()` 复用同一套飞出编排；后端 1 小时同卡防重复 |
| 全屏专注 | `document.documentElement.requestFullscreen()` + `body.kb-review-immersive` 全局类隐藏顶栏 / 去内容区留白 / 压暗底色（`--kb-immersive-bg`）。**不使用遮罩层**，故 Toast（z-50）、`1/2/3/4`、`0` 快捷键、浏览器原生 Esc 退出全部照常；`fullscreenchange` 同步状态，组件卸载时兜底移除类名并退出全屏 |
| 复习热力图 | `components/ReviewHeatmap.vue`：7 行（周一~周日）× 7 列固定网格。请求天数按今天星期动态算（`49 - (6 - mondayIndex(today))`，43~49 天），保证**首日恰为周一、今天落在最后一列**，本周未来格子留空。5 级绿色阶梯由 `color-mix(--kb-accent, --kb-muted)` 生成，附活跃天数与连续打卡 streak |
| 遗忘曲线 | **已上移到复习驾驶舱**（`/workbench/review`）的折叠面板「📊 近 N 天遗忘趋势」，14/30/90 天切换，共用 `WorkbenchReview.vue` 的 SVG 双图层（柱=复习量、折线=遗忘率），首次展开才懒加载；`/review` 闪卡页只保留进度条与卡型徽章，刷题中不被数据分心 |

**卡片正反面映射**

| 源 | 正面（front） | 背面（back） |
|----|--------------|-------------|
| `wb_note` | `cue_column`（线索列），空则用 `title`，再空则「未命名笔记」 | `note_column` + `summary_column` 拼接 |
| `wb_palace_loci` | `name`（位点名称），空则「未命名位点」 | `knowledge_point` |

**前端实现**：`views/Review/index.vue` + `views/Review/FlashCard.vue`（3D 翻转，卡背复用 DocLibrary 的 `renderMarkdown` 做安全渲染）；状态走 `store/reviewStore.ts`（Pinia）。

> **队列推进的取巧写法**：`reviewStore` 让 `currentIndex` 恒为 0，每答完一张就 `splice(0, 1)` 让下一张自然滑入 index 0。这样卡片进出动画只需盯住一个固定索引，避免 index 递增导致的 key 复用与动画错乱。

### 7.13 ★ 全局命令面板 `Cmd/Ctrl + K`（v1.1.0 新增）

任意页面按 `⌘K`（Windows/Linux `Ctrl+K`）唤起，跨模块秒搜秒跳。

**能力**

- 跨 **收集箱 / 康奈尔笔记 / 费曼故事** 三类内容实时模糊检索（`GET /api/search?q=`）
- 结果卡展示类型徽章 + 标题 + 正文前 30 字摘要
- `↑` / `↓` 键盘导航，`Enter` 跳转，`Esc` 关闭
- 输入 **500ms 防抖**（`useDebounceFn`），避免每敲一个字都打后端

**实现**

| 层 | 文件 | 说明 |
|----|------|------|
| 状态 | `stores/searchStore.ts` | Pinia setup store：`isOpen` / `query` / `results` / `loading` + `openPalette` / `closePalette` / `performSearch` |
| UI | `components/CommandPalette.vue` | `<Teleport to="body">` + `<Transition>` 毛玻璃遮罩 + 600px 卡片 |
| 快捷键 | `App.vue` | `window` keydown 监听：`(metaKey \|\| ctrlKey) + k` 切换、`Esc` 关闭 |
| 入口 | `DesktopTopNav.vue` | 右侧「搜索 ⌘K」按钮复用 `openPalette()` |

### 7.14 ★ 工作台首页动态化（v1.1.0）

见 §7.1。原首页的闭环四步气泡与今日聚焦四卡都是静态占位数字，本轮接入 `GET /api/dashboard/stats` 全部替换为真实统计，并保证「气泡数字」与「点进去看到的内容量」一致。

### 7.15 ★ 新手引导 `/onboarding` 与设置中心 `/settings`（v1.1.0 新增）

**新手引导** — 首次启动强制走一遍（路由守卫门禁，见 §6.1）：

1. 欢迎与产品理念介绍
2. 选择知识库数据目录（当前作为偏好记录，见 §4.4 说明）
3. 配置 AI 网关（API 地址 / Key / 模型），**可跳过**，跳过后 AI 能力优雅降级
4. 完成 → `POST /api/config/init` 落盘，`hasOnboarded = true`，进入工作台

**设置中心 `/settings`** — 引导完成后的常驻配置入口，与引导页**共用同一套 store 动作与后端端点**（`useAppStore.persistConfig()` → `POST /api/config/init`），保证两处改配置行为完全一致。额外提供「重新运行新手引导」按钮，跳转 `/onboarding?rerun=1`（守卫对 `rerun=1` 放行）。

**安全约定**：`useAppStore` 的 `persist.pick` **只持久化引导状态与非敏感设置**，明文 `apiKey` 绝不写入 `localStorage`；提交成功后立即清空内存中的明文 Key，后续回显一律走后端掩码值。

---

### 7.16 ★ 番茄钟 `/pomodoro` + `/pomodoro/stats`（2026-08-07 新增）

独立的专注计时模块，**计时引擎常驻后台**（Pinia `pomodoroStore` 应用级单例），用户切去复习 / 笔记页也不会中断。

**后端（`/api/pomodoro`，5 个端点，落 `wb_pomodoro_log` 表）**：

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/pomodoro/record` | 记录一次已完成的专注 / 休息。body `{ type: 'work'\|'short_break'\|'long_break', duration: number(秒) }`，服务端用 `now - duration` 反推 `start_time`/`end_time`，写 `wb_pomodoro_log` |
| GET | `/api/pomodoro/stats?days=7` | 按天聚合（本机时区 `YYYY-MM-DD` 分桶，缺记录补 0 行），返回 `points[{date,work,shortBreak,longBreak,breakTotal,workCount}]` + `summary{totalWorkSeconds,totalBreakSeconds,avgWorkSecondsPerDay,completedPomodoros}` |
| GET | `/api/pomodoro/config` | 读取偏好 `<dataDir>/pomodoro-config.json` |
| PUT | `/api/pomodoro/config` | 局部更新偏好（`settings` / `soundSettings` / `whiteNoise` / `autoStartNext` 任意子对象只带部分字段），字段校正 + 夹值 |
| GET | `/api/pomodoro/today` | 今日速览 `{ workSeconds, pomodoros }` |

**前端**：

- `src/store/pomodoroStore.ts` — 计时引擎（时间戳差值法：`accumulatedMs + (Date.now() - runStartedAt)`，setInterval 仅 250ms 刷新进度环）、阶段机（work → 短/长休息，`currentCycle` 与 `cyclesPerSet` 联动）、配置读写（防抖 + 后端水合）、音频 actions、菜单栏联动 `invoke('update_tray_title', { title })`（emit `tray:update` 兜底，阶段 emoji + MM:SS，每秒节流）、结束 `invoke('trigger_notification')`。
- `src/views/Pomodoro/index.vue` — 主页面：conic-gradient 圆形进度环 + 四参数配置 + 控制按钮（开始/暂停/重置/跳过/退出）+ 白噪音 Mini 播放器（Howler 播放 `public/audio/white-noise/*.mp3`，缺失降级 Web Audio 合成）+ 提示音设置（Web Audio 合成 ding/tick/alarm）。
- `src/views/Pomodoro/PopupTimer.vue`（**菜单栏弹窗专用**，2026-08-07 晚间新增，后经「Harmony Glow / 光影辉光」重设计）— 无边框 / 透明 / 三重毛玻璃面板（环境动态光晕层 blur + `backdrop-blur` 玻璃层 + 内高光描边），由 `pomodoro_popup` 窗口（380×460、`always_on_top`、失焦自动隐藏）渲染；主视觉「时光之环」：4px 深色底座环 + 4px 亮色进度环（`stroke-dashoffset` 线性连续）+ 进度点「太阳耀斑」柔光，环中央极细化巨型倒计时（`font-thin` + `tabular-nums` + 多层 `text-shadow` 辉光）与胶囊状态标签；顶栏极小化（状态点 + 阶段文字 / 幽灵图标按钮），底部悬浮无边框图标按钮（开始 / 暂停 / 重置 / 跳过）+ 左下角白噪音状态浮标；阶段色映射（专注珊瑚橙 / 小憩海洋蓝 / 长休薄荷绿）经 `@property` 注册为 `<color>` 自定义属性实现 0.8s 无缝过渡，跟随系统 light/dark。
- `src/views/Pomodoro/Stats.vue` — 统计页：vue-chartjs 柱状图（蓝 = 专注 / 橙 = 休息）+ 累计专注时长 / 日均专注 / 完成番茄数三张总结卡。
- `src/api/pomodoro.ts` — 类型与请求封装；`src/utils/pomodoroAudio.ts` — 提示音（Web Audio 合成）+ 白噪音（Howler + 合成兜底）。

**复习页集成**：`/review` 顶部状态条（`src/views/Review/index.vue`）绑定 `pomodoroStore`，刷题中可见剩余时间并一键暂停 / 继续。

**macOS 菜单栏（状态栏）应用形态（2026-08-07 晚间重构为弹窗式）**：启动即只显示状态栏托盘图标 + 文本倒计时，主窗口默认隐藏；计时引擎在渲染进程（`pomodoroStore`）常驻，即便弹窗被 `hide()`、WebView 仍执行 JS，时间戳差值法保证后台节流 / 休眠不掉秒，托盘标题照常更新。

- **菜单栏倒计时（图标位图，非 set_title）**：`tray.rs::paint_tray_title` 把「阶段色圆点 + MM:SS」用 5×7 点阵烤成透明 RGBA 图标位图并 `set_icon`。**为何不用 `set_title`：Tauri 2 在部分 macOS 版本上初始标题能显示、但运行时反复 `set_title` 不触发状态栏重绘（正是最早「菜单栏永远停在 🍅 25:00」的根因）；而 `set_icon` 必然触发 NSStatusItem 重绘，是最可靠通道。** 文本用「白字+1px 暗描边」适配浅/深色菜单栏。前端 store 每秒主用 `invoke('update_tray_title', { title })` 命令（Rust `paint_tray_title` 渲染），`tray.rs` 的 `app.listen('tray:update')` 作事件通道兜底，二者共用同一套图标渲染。
- **左键点击托盘 → 弹窗**：`on_tray_icon_event` 命中 `MouseButton::Left` 调 `toggle_popup()`——`pomodoro_popup` 可见则 `hide()`，否则 `center()+show()+set_focus()`；右键弹原生菜单（显示主窗口 / 退出）。`show_menu_on_left_click(false)` 让左键只触发点击、右键才出菜单。
- **弹窗窗口**：`lib.rs` 在 `setup` 用 `WebviewWindowBuilder`（与主窗口同款，因本项目窗口均在代码中创建）建 `pomodoro_popup`——`decorations(false)` + `transparent(true)`（需 `tauri` 的 `macos-private-api` feature）+ `always_on_top(true)` + `resizable(false)` + 固定 `inner_size(380,460)` + `center()` + `visible(false)`。`on_window_event` 监听 `WindowEvent::Focused(false)`，延迟 200ms 且二次校验焦点（叠加 350ms 宽限期去抖挡 macOS 点击托盘的伪失焦）后 `hide()`，形成「点击外部关闭」的下拉面板体验。
- **后台运行（无需主窗口）**：`setup` 启动即 `get_webview_window("main").hide()`（菜单栏应用形态）；关闭主窗口 `WindowEvent::CloseRequested` 拦截 `prevent_close()+hide()` 同样不退出。`activationPolicy` 在 macOS 设为 `Accessory`（无 Dock 图标）。
- **结束原生通知**：阶段自然结束前端 `store.emitFinished()` → `invoke('trigger_notification', { title, body })` 命令，Rust `#[tauri::command] fn trigger_notification` 经 `tauri-plugin-notification` 弹 macOS Notification Center 提醒（应用命令在 Tauri 2 默认放行，无需额外 capability）。
- **App 菜单「番茄钟」项**：`timer_menu` 点击切换 `pomodoro_popup` 弹窗（与状态栏托盘同款）；顶栏「番茄钟」导航项 `popup: true` 拦截默认跳转，`WebviewWindow.getByLabel('pomodoro_popup').show()` + 隐藏主窗口，实现从主应用无缝进入菜单栏形态。
- **多窗口 Store 说明**：主窗口与弹窗各有独立 WebView / 独立 `pomodoroStore` 实例；菜单栏计时以弹窗内的实例为准（其 JS 在隐藏态仍存活）。`App.vue` 两个窗口都 `init()` 计时引擎。

> 计时精度要点：后台标签页 / 系统休眠时 setInterval 会被节流，故计数只依赖时间戳差值，掉帧不影响结果；配置必须持久化到后端 JSON（`<dataDir>/pomodoro-config.json`），不可只存内存。

> 计时精度要点：后台标签页 / 系统休眠时 setInterval 会被节流，故计数只依赖时间戳差值，掉帧不影响结果；配置必须持久化到后端 JSON（`<dataDir>/pomodoro-config.json`），不可只存内存。

---

## 8. AI 能力体系

### 8.1 设计边界（重要）

AI 层是**旁挂式**的，三条硬约束：

1. **不侵入既有契约** — AI 路由独立挂 `/api/ai`，不改动任何 `/api/workbench/*` 端点
2. **只算不存** — AI 只负责「算出结果返回前端」，落库仍走原有的 `PUT /notes/:id`、`PUT /stories/:id`、`POST /recall-sessions/:id/submit`
3. **全程可选** — 关掉 AI 或不配 Key，主流程功能完全不受影响

### 8.2 配置与降级

配置文件 `<dataDir>/ai-config.json`（权限 600，`src-api/data/` 已 gitignore）。

| 配置项 | 默认值 | 说明 |
|--------|-------|------|
| `enabled` | true | 总开关 |
| `provider` | deepseek | deepseek / openai / custom |
| `baseUrl` | `https://api.deepseek.com/v1` | OpenAI 兼容根地址，不含 `/chat/completions` |
| `model` | `deepseek-chat` | — |
| `temperature` | 0.3 | 0~2，越低越稳定 |
| `timeoutMs` | 45000 | 5000~180000 |
| `embeddingsBaseUrl/ApiKey/Model` | 空 | 与聊天服务解耦，按需单独配置 |

环境变量 `KNOWFLOW_AI_KEY` / `KNOWFLOW_AI_MODEL` / `KNOWFLOW_AI_BASE_URL` 优先级最高（便于 CI 与临时覆盖）。

**实现要点**：只用 Node 22 原生 `fetch`，**不引入任何 LLM SDK**，保持体积与离线可用性；协议统一走 OpenAI 兼容的 `/chat/completions`，切换服务商只改配置。

**错误码体系**（`LlmError`，前端据 `aiCode` 决定是否引导去设置页）：

| aiCode | 含义 |
|--------|------|
| `AI_DISABLED` | 总开关关闭 |
| `AI_NOT_CONFIGURED` | 未配置 Key |
| `AI_TIMEOUT` | 请求超时 |
| `AI_UPSTREAM_ERROR` | 上游服务报错 |
| `AI_BAD_RESPONSE` | 模型返回无法解析为预期 JSON |

**输出规整**：`chatJson` 强制 JSON 输出，路由层再做 `normalizeScore`（收敛 0~100 整数）、`normalizeList`（数组/字符串统一成字符串数组并限长）等兜底，防止模型格式漂移把前端搞崩。

### 8.3 AI 端点清单（21 个，前缀 `/api/ai`）

**配置与探针（4）**

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/config` | 读配置（Key 脱敏）+ 服务商预设列表 |
| PUT | `/config` | 写配置 |
| POST | `/test` | 连通性测试 |
| GET | `/status` | 各项 AI 能力是否可用 |

**内容加工（9）**

| 路径 | 归属模块 | 作用 |
|------|---------|------|
| `POST /capture/summarize` | 收集箱 | 提炼摘要要点 |
| `POST /tags` | 收集箱 | 标签建议 |
| `POST /capture/draft-note` | 收集箱→笔记 | 生成康奈尔三栏草稿 |
| `POST /note/generate` | 笔记 | 由笔记列生成线索列/总结列 |
| `POST /note/flashcards` | 笔记→复习 | 批量生成问答卡 |
| `POST /story/draft` | 笔记→故事 | 生成费曼故事初稿 |
| `POST /story/clarity` | 故事 | 清晰度评分 + 建议 |
| `POST /palace/loci` | 宫殿 | 位点与图像联想生成 |
| `POST /palace/loci/image-hint` | 宫殿 | 单个位点的图像联想提示（增强版 image_hint） |

**评估与洞察（5）**

| 路径 | 作用 |
|------|------|
| `POST /recall/score` | 主动回忆语义评分（附规则分对照） |
| `POST /recall/advice` | 回忆改进建议 |
| `POST /insight/report` | 学习周报 |
| `POST /weakness/diagnose` | 薄弱点诊断 |
| `POST /review/recommend` | 复习优先级推荐 |

**向量检索（2）**

| 路径 | 作用 |
|------|------|
| `POST /embeddings/sync` | 全量/增量重建向量索引（`content_hash` 未变则跳过） |
| `POST /associate` | 按余弦相似度查找关联内容，返回标题、摘要、相似度、跳转路由 |

> 注：上文「AI 端点 21 个」已包含 `POST /api/ai/generate-mindmap`（思维导图 AI 生成，由 `mindmapAiRoutes` 注册，与 §7.11 思维导图 CRUD 的 5 端点分开计数）。

> Prompt 模板集中在 `src-api/src/lib/prompts.ts`（882 行），15 组 `buildXxxPrompt` 函数各自配套 TS 输出类型（如 `RecallScoreOutput`、`InsightReportOutput`、`MindMapOutput`），前后端类型闭环。
>
> 分阶段规划（P0 管线 → P1 高回报 → P2 体验 → P3 进阶）详见同目录 `AI功能增强方案.md`。

---

## 9. 桌面外壳原生能力

`src-tauri/src/lib.rs`（约 760 行）在薄壳基础上提供四项能力，**业务代码零改动**。

### 9.1 原生菜单

macOS 标准菜单栏：

- **KnowFlow**（App 菜单）：关于 / 检查更新… / 去学习复习 / 复习提醒：开（可切换） / 退出
- **视图**：重新加载页面（等效 `location.reload()`）

「去学习复习」会 `emit("navigate", "/review")` 到渲染进程，前端导航层（DesktopTopNav / App 根组件）监听该事件后 `router.push('/review')`（间隔复习页，见 §7.12）。

> 注：间隔复习相关路由实际为 `/review`（新系统闪卡）、`/review/flashcard`（闪卡专注模式别名）、`/workbench/review`（复习驾驶舱，双系统总入口）、`/workbench/review/card-list`（旧系统传统卡组）；顶栏「复习」项经 `match` 数组把以上路径统一高亮。

### 9.2 复习提醒通知

- 独立后台线程，每 **30 分钟**轮询 `GET /api/workbench/reviews/due-count`
- 有待复习卡片则弹 macOS 原生通知，正文含数量与最多 3 条卡面样例
- 菜单项可随时开关，状态由 `Arc<Mutex<bool>>` 在菜单与调度线程间共享
- 轮询走 `ureq`（纯 Rust HTTP，无 OpenSSL 依赖），仅请求计数端点，开销极小

### 9.3 自动更新

- 集成 `tauri-plugin-updater`，菜单「检查更新…」与前端顶栏按钮共用 `check_for_update` 命令
- 检测到新版本自动下载安装，完成后弹通知
- **当前 `updater.active = false`**：启用需自备 ed25519 签名密钥与发布端点（属个人密钥，不入库）

### 9.4 进程生命周期治理（v1.1.0 重构）

v1.1.0 起，Node 侧车不再走 `tauri-plugin-shell` 的 sidecar API，而是用 `std::process::Command` 直接拉起，由宿主自管的 `SidecarManager`（`src-tauri/src/lib.rs`，约 760 行）掌控其完整生命周期。核心目标有二：① 崩溃后**无限自愈重启**（开发模式除外）；② 端口**恒定不变**，否则已加载页面的断线重连会永远连不回来。

**启动链路**

1. `SidecarSpec` 固化启动参数（`node_bin` / `entry` / `web_dir` / `data_dir` / `port` / `log_file`），重启时原样复用。
2. `spawn_process()` 拉起子进程：注入 `KNOWFLOW_DATA_DIR` 与 `--data-dir`（双写，兼容新老后端）、`KNOWFLOW_PORT`、`NODE_ENV=production`；`cwd` 设为可写数据目录（防止任何库按相对路径落盘到只读的 `.app`）；stdout/stderr 接管后逐行转发到宿主控制台并落盘 `<data>/logs/sidecar.log`。
3. `supervise()` 在**独立线程**里阻塞 `child.wait()`，主线程一秒都不被挡；就绪探测另开线程 `wait_for_backend` 后 `emit("sidecar://ready")`。

**自愈重启策略（独立线程无限循环）**

| 阶段 | 行为 |
|------|------|
| 端口释放 | 拉起前先 `wait_port_released`（最长 10s）确认端口空闲，避免撞 `EADDRINUSE` |
| 退避重启 | 异常退出后 `sleep(delay)`，`delay` 从 `RESTART_BASE_DELAY=2s` 起翻倍、封顶 `RESTART_MAX_DELAY=30s`（防进程炸弹） |
| 重置退避 | 子进程存活 ≥ `HEALTHY_UPTIME=30s` 才视为「跑着跑着崩」，退避计时归零 |
| 正常退出不重启 | 退出码 0（含孤儿自检主动退出）直接退出监控，不再拉起 |
| 停止信号 | 应用退出时 `stopping` 置位，监控线程不再重启 |

**应用退出回收**

`shutdown()` 先发 `SIGTERM`（-15）给子进程留出清理窗口，2 秒内未走再 `SIGKILL`（-9）强杀；kill 走系统 `/bin/kill`（不引入 libc 依赖）。不回收会堆积僵尸后端占用端口。

**前端可触发的手动重启**

`restart_sidecar` Tauri 命令（连接遮罩「重启服务」按钮调用）对当前 pid 发 `SIGTERM`，监控线程随即接管自愈；开发模式下返回 `restarting:false` 并提示后端由 `npm run dev:api` 托管。

**后端侧辅机制**

- 孤儿自检：`src-api/src/index.ts` 每 5s 检查 `process.ppid === 1`（被 launchd 收养 = 宿主没了），主动 `process.exit(0)`。
- 宿主模式端口不漂移：后端经 `isLaunchedByHost()` 识别由 Tauri 拉起后，遇 `EADDRINUSE` 只等待旧实例释放、端口**恒定不变**；独立开发模式（`npm run dev:api`）才保留 +1 漂移。

### 9.5 权限声明

`src-tauri/capabilities/default.json`：

```json
["core:default", "shell:allow-execute", "shell:allow-spawn",
 "notification:default", "updater:default", "dialog:default"]
```

侧车启用靠 `bundle.externalBin` + `shell:allow-spawn`，**不是**靠 `plugins.shell` 配置（见 §12 踩坑）。`dialog:default` 供引导页 / 设置中心的「选择文件夹」命令（`select_directory`，§7.15）打开系统原生目录选择弹窗。

---

## 10. 开发、构建与发布

### 10.1 常用命令

| 命令 | 作用 |
|------|------|
| `npm run dev:all` | 同时起前端 5173 与后端 8787，浏览器开发（**无需 Rust**） |
| `npm run dev:web` / `dev:api` | 单独启动某一侧 |
| `npm run build:all` | 构建前端 dist + 后端 dist |
| `npm run tauri:dev` | Tauri 开发模式（窗口加载 vite dev server） |
| `npm run tauri:build` | 打包 macOS `.app` |

首次准备：

```bash
npm --prefix src-api install
npm --prefix src-ui install
```

### 10.2 打包完整流程（需 Rust 工具链）

```bash
# 1. 安装 Rust（一次性）
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh && source "$HOME/.cargo/env"

# 2. 安装 Tauri CLI
npm install

# 3. 生成 Node 侧车二进制（复制本机 node → binaries/server-<target-triple>）
bash scripts/prepare-bin.sh

# 4. 生成图标（准备 1024×1024 png）
npm run tauri icon /path/to/icon.png

# 5. 构建
npm run tauri build
```

产物：`src-tauri/target/release/bundle/macos/KnowFlow 学习工作台.app`（arm64，约 220MB）。

### 10.3 打包资源清单

`tauri.conf.json` 的 `bundle.resources` **四项缺一不可**：

| 源 | 目标 | 缺失后果 |
|----|------|---------|
| `../src-ui/dist` | `web` | 无前端页面 |
| `../src-api/dist` | `api` | 无后端代码 |
| `../src-api/node_modules` | `api/node_modules` | `better-sqlite3` 原生模块 require 失败 |
| `../src-api/package.json` | `api/package.json` | **GUI 启动白屏**（历史事故） |

### 10.4 运行未公证的 .app

本机构建产物是 ad-hoc 签名（主程序），侧车 `server` 复用 Node.js 官方 Developer ID 签名。本机双击通常可开；被 Gatekeeper 拦截时：

```bash
xattr -cr "src-tauri/target/release/bundle/macos/KnowFlow 学习工作台.app"
open "src-tauri/target/release/bundle/macos/KnowFlow 学习工作台.app"
```

对外分发必须做 Apple Developer ID 签名 + `notarytool` 公证。

### 10.5 启用自动更新（发布前一次性）

```bash
npx tauri signer generate          # 私钥离线保管
# 公钥填入 tauri.conf.json 的 plugins.updater.pubkey，并把 active 改为 true
# endpoints 改成自己的仓库地址
```

---

## 11. 约定与规范

### 11.1 数据库规范（阿里巴巴开发手册）

- **不使用物理外键与级联**，表间关联一律逻辑外键（仅存 `user_id` / `note_id` / `category_id` 等列），完整性由应用层 Service 维护
- 表名小写非复数、必备 `id` / `created_at` / `updated_at`
- 逻辑外键列应建索引

### 11.2 前端规范

- 基线：Airbnb JavaScript Style Guide + Vue 3 官方风格指南（Priority A/B）
- 组件统一 `<script setup lang="ts">`
- **所有样式收敛到 `--kb-*` token**，禁止在组件内硬编码色值 / 圆角 / 字号

### 11.3 UI 一致性六原则（本项目长期约定）

开发界面时**整体视觉风格不得改变**，须满足：

1. 布局与间距统一，视觉层次清晰
2. 字体大小 / 字重 / 行高统一，标题与正文层级分明
3. 所有可交互元素具备 hover / active / focus 状态反馈
4. 图标风格与尺寸统一
5. 窗口圆角、阴影、边框符合 macOS 设计习惯
6. 不同分辨率与 DPI 下不错位、不溢出

### 11.4 构建验证纪律（血泪教训）

- **`vue-tsc --noEmit` 通过 ≠ 能打包**。CSS `import` 路径错误 vue-tsc 查不出来，必须真跑一次 `vite build` / `npm run tauri:build`
- 前端类型检查用 `src-ui/node_modules/.bin/vue-tsc`，后端用 `src-api/node_modules/.bin/tsc`，**禁止 `npx`**
- 本地调后端 curl 要加 `--noproxy '*'`，否则回环请求会被代理拦截

---

## 12. 已知限制与踩坑清单

### 12.1 产品级限制

| 限制 | 说明 |
|------|------|
| 单用户无鉴权 | `CURRENT_USER = 1` 硬编码，不支持多账户 |
| 仅 macOS | 未适配 Windows / Linux 分发链路 |
| 端口范围 | 宿主模式端口**恒定**（8787 起优先选，仅绑 `127.0.0.1`）；独立开发模式 `npm run dev:api` 才保留 +1 漂移（最多 20 个） |
| Mac App Store | 不推荐上架，本地 SQLite + 文件访问受沙箱限制 |
| 通知点击不跳转 | `tauri-plugin-notification` 2.3.x 桌面端 builder 无 `on_click` 回调，点击仅原生聚焦应用 |
| 数据导入一次性 | 目标库已有数据时 `POST /import` 直接拒绝 |

### 12.2 历史事故复盘

**① 双击 .app 无任何界面（2026-08-06）**

- 现象：Dock 图标弹一下即消失，完全没有窗口
- 根因：`plugins.shell` 里写了非法字段 `execute` / `sidecar`。`tauri-plugin-shell` 2.3.5 的 Config 只有 `open` 一个字段且 `deny_unknown_fields`，插件初始化阶段直接 panic，**发生在任何窗口创建之前**，所以连白屏都没有
- 修复：`plugins.shell` 只保留 `"open": false`
- 排查手法：终端直接运行主程序看 panic 堆栈

```bash
"src-tauri/target/release/bundle/macos/KnowFlow 学习工作台.app/Contents/MacOS/knowflow-desktop"
```

**② 双击白屏、终端启动却正常（经典 macOS launchd 陷阱）**

- 现象：`.app` 双击后页面白屏，侧车 `server` 进程活着但 8787 **永不监听**；同一个侧车命令在终端手动执行一切正常
- 根因：bundle 的 `Resources/api/` 缺少 `package.json`。Node 启动时会从脚本目录**向上逐级查找最近的 `package.json`** 以确定 CommonJS/ESM 作用域（`GetNearestParentPackageJSONType → TraverseParent`）。侧车由 launchd 拉起时 `cwd=/`，查找链一路爬到根目录；在 launchd/沙箱环境下最后那次 `open()` **永久阻塞**而不是返回 ENOENT。终端启动因环境完整会立即 ENOENT，故表现出「终端能跑、双击白屏」
- 定位手段：`sample <pid> 1` 抓到侧车卡在 `uv_fs_open → open()` 调用栈
- 修复：`bundle.resources` 增加 `"../src-api/package.json": "api/package.json"`，让 Node 在 `api/` 层就找到作用域、不再上爬
- 教训：**Tauri 把 Node 作为 sidecar 打包时，务必保证 `node_modules` 同级有 `package.json`**。进程「活着但不干活」这类问题，用 `sample` / `spindump` 抓调用栈最快
- 二次排查路径：① `lsof -i:8787` 看端口是否监听；② 手动跑侧车验证

```bash
Contents/MacOS/server Contents/Resources/api/index.js \
  --port 8787 --web-dir Contents/Resources/web --data-dir /tmp/test
```

**③ 重新打包后 `/assets/*` 永久 404**

- 根因：`@fastify/static` 曾用 `wildcard: false`，它在注册时枚举目录树建路由；重新打包后 assets 目录 inode 变化，常驻进程仍指向已消失的旧目录
- 修复：改回默认通配路由

**④ WKWebView 加载模块脚本静默失败**

- 根因：Vite 产物的 `<script type="module">` 带 `crossorigin`，WKWebView 加载本地回环地址仍按 CORS 模块处理
- 修复：静态响应显式回写 `Access-Control-Allow-Origin` 与 `Vary: Origin`

**⑤ SPA 回退把缺失的 .js 变成 index.html**

- 现象：白屏且控制台无任何报错，极难排查
- 修复：`setNotFoundHandler` 中对带扩展名的资源（js/css/map/图片/字体/wasm 等）如实返回 404，只有页面路由才回退 `index.html`

**⑥ Tauri updater API 签名坑**

- `download_and_install(on_chunk, on_download_finish)` 需要**两个**闭包
- `PredefinedMenuItem::about` 需要第三个 `AboutMetadata` 参数（传 `None`）

**⑦ axios 断线重放不能用 `request.request(error.config)`（v1.1.0）**

- 现象：重连后业务请求永久卡死、落到 404，前端一直显示「重新连接中」
- 根因：axios 拦截器里若写 `return request.request(error.config)`，派发流程会改写 `error.config`（`baseURL` 合并、url 可能被写成绝对地址），再原样重放会触发「绝对地址被当成请求行原样发出 / URL 前缀翻倍」等异常，重放请求错到不知哪去
- 修复：新增公共方法 `replayRequest(config)`，只取 `method/url/data/params/headers` 等干净字段重新发起，绝不二次经过 `request.request` 的合并逻辑（见 §6.6）

**⑧ 本地调后端 curl 必带 `--noproxy '*'`（常踩）**

- 本机若配了 `HTTP_PROXY` / `HTTPS_PROXY`，curl 打 `127.0.0.1:8787` 的回环请求会被代理拦截，表现为「后端明明起来了但请求超时」
- 修复：`curl --noproxy '*' http://127.0.0.1:8787/api/health`，或在脚本里 `unset http_proxy https_proxy`

---

## 附：文档索引

| 文档 | 内容 |
|------|------|
| `README.md` | 快速开始、打包步骤、原生能力说明、构建验证记录 |
| `AI功能增强方案.md` | AI 能力的模块级机会梳理与 P0~P3 实施路线图 |
| `技术架构与功能手册.md` | 本文档：架构、数据、接口、功能、规范、踩坑 |
| `.workbuddy/memory/` | 项目长期记忆与每日工作日志 |
