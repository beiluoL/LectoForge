# 流程图 AntV X6 自研升级 — 交付检查清单（Checklist）

> 文档版本：v1.0 · 关联规格：[spec.md](./spec.md) · 任务清单：[tasks.md](./tasks.md)
>
> 使用规则：
> - 每完成一个任务 → 在对应行「任务完成标记」签勾（✅/❌）+ 完成人 + 完成时间
> - 每个勾必须有**验证方法**，不能「感觉可以」就勾
> - 最终交付前必须 Phase 1 全绿，Phase 2 至少 P0/P1 项全绿（P2 允许有勾后补）

---

## 0. 环境与工程检查（Pre-Flight — 开工前必勾）

| # | 检查点 | 验证方法 | 任务 | 结果 | 完成人 | 时间 |
|---|---|---|---|---|---|---|
| ENV-01 | 本地 Node 版本 ≥ 18，npm 版本 ≥ 8 | `node -v` + `npm -v` | P1-T1.1 |  |  |  |
| ENV-02 | `npm install` 执行完毕，exit code = 0，无 dependency conflict 警告 | 运行后控制台无红色 ERR / WARN peer | P1-T1.1 |  |  |  |
| ENV-03 | `@vue-flow/*` 完全从 package.json 移除，`npm ls @vue-flow/core` 返回 "Not found" | 执行命令看输出 | P1-T1.1 |  |  |  |
| ENV-04 | 12 个 X6 插件版本号主版本一致（如都是 2.x.y，不出现 1.x / 3.x 混用） | `npm ls @antv/x6 @antv/x6-plugin-history @antv/x6-vue-shape ...` 输出比对 | P1-T1.1 |  |  |  |
| ENV-05 | `npm run dev` 启动 1 分钟内控制台无 X6 红色报错、无 runtime stacktrace | 手工启动观察 + Chrome DevTools Console 截图 | P1-T1.1 |  |  |  |
| ENV-06 | TypeScript `npm run typecheck` 通过，无 `any` 新增注入 X6 JSON 类型 | 运行 typecheck 截图 | P1-T1.2 |  |  |  |
| ENV-07 | ESLint `npm run lint` 通过（vue/ts 规则 + Airbnb 强制项） | 运行 lint 截图 | P1-T1.2 |  |  |  |
| ENV-08 | `npm run dev` 启动，`/api/diagram/*` 路由可用（200） | curl 或浏览器访问 healthz | P1-T5.5 |  |  |  |

---

## 1. Phase 1 验收（必勾）

### 1.1 工程改造 & 骨架（P1-T1）

| # | 检查点 | 验证方法 | 对应任务 | 结果 | 完成人 | 时间 |
|---|---|---|---|---|---|---|
| P1-01 | `x6/useGraph.ts` composable 能返回 graph 实例，挂载后能 `addNode` 画矩形 | 在 play.vue 单测页面渲染一个最小 Graph | P1-T1.2 |  |  |  |
| P1-02 | 组件 onUnmounted 后 `graph.dispose()` 调用，Chrome Memory 拍快照无 Graph/Event 残留 | DevTools Memory 拍 2 张快照前后比对（create → destroy → GC → Snapshot 无 Graph 残留） | P1-T1.2 |  |  |  |
| P1-03 | `graphConfig.ts` 配置生效：grid(8×8) 能看到网格；history Ctrl+Z 可用；snapline 拖两矩形接近出粉色虚线；minimap 右下可见；框选选中多节点 | 每一项手动截图留存 | P1-T1.3 |  |  |  |
| P1-04 | `vue-shapes.ts` 测试节点 `test-vue-node` 能渲染红色文字 hello，Vue 响应式正常（改 prop 文字变） | 用 Pinia 控制 prop text 变化 | P1-T1.4 |  |  |  |

### 1.2 节点与连线（P1-T2）

| # | 检查点 | 验证方法 | 对应任务 | 结果 | 完成人 | 时间 |
|---|---|---|---|---|---|---|
| P1-05 | 11 种形状（rect / rounded / ellipse / diamond / hexagon / terminal / process / decision / class / interface / note）视觉与旧 Vue Flow 实现像素差 ≤ 2px | 并排截图 11 张做 diff | P1-T2.1 |  |  |  |
| P1-06 | 每个形状 label 位置（水平/垂直居中、padding）与旧版一致 | 截图对比 label 位置 | P1-T2.1 |  |  |  |
| P1-07 | 双击节点 → contenteditable 生效 → Enter 保存 / Esc 取消 / 失焦自动保存；文字存 `attrs.label.text` | 手工 3 次编辑流程录屏 | P1-T2.2 |  |  |  |
| P1-08 | 四向锚点 ports 可从 top/right/bottom/left 拖出连线，松手自动连接目标节点最近 port | 各方向拉一次 | P1-T2.2 |  |  |  |
| P1-09 | NodeResizer：最小 60×36 不允许更小；松手触发 history（拖大再 Ctrl+Z 1 步回） | 拖拽 50×20 被阻止，≥60×36 正常 | P1-T2.2 |  |  |  |
| P1-10 | 选中态 2px 加粗描边 + 外阴影（非选中恢复） | 选中 2 节点 → 取消 → 取消后无残留样式 | P1-T2.2 |  |  |  |
| P1-11 | 3 种线型（smoothstep/bezier/straight）切换视觉一致；命中区 ≥24px（细线点中无问题） | WKWebView 真机上点边测试 | P1-T2.3 |  |  |  |
| P1-12 | 边选中态出现编辑文字按钮 + 删除按钮；点删除 Ctrl+Z 1 步恢复 | 按钮点两次测试 | P1-T2.3 |  |  |  |
| P1-13 | 边可拖动拐点生成 waypoint；可右键菜单「添加/删除航点」 | 折线边拖拐点 3 次 | P1-T2.4 |  |  |  |
| P1-14 | 终点箭头 6 种样式（open / closed / diamond / block / classic / ER-diamond）切换生效；起点箭头同样可选 | 属性面板下拉切换 | P1-T2.4 |  |  |  |
| P1-15 | 锚点吸附：连接端点贴节点边缘（不穿内部）；属性面板可切换"仅中心"vs"四向锚点" | 视觉验证端点位置 | P1-T2.4 |  |  |  |

### 1.3 核心编辑能力（P1-T3）

| # | 检查点 | 验证方法 | 对应任务 | 结果 | 完成人 | 时间 |
|---|---|---|---|---|---|---|
| P1-16 | 撤销重做：10 步典型动作（加节点 / 连线 / 移动 / 改色 / 改文字），每步 Ctrl+Z 可逐一回滚；栈顶空时按钮 disabled | 录制 10 步后全回 | P1-T3.1 |  |  |  |
| P1-17 | undo 栈 max=50：做 60 步后回滚，第 1 步不可再回（栈底丢弃正确） | 计数校验 | P1-T3.1 |  |  |  |
| P1-18 | 复制粘贴：选 2 节点 + 1 边 → Ctrl+C / V → 新节点 offset (24,24) 不重叠；跨页复制 → Page A Copy，Page B Paste，节点在 B 页出现 | 手工 2 场景测试 | P1-T3.2 |  |  |  |
| P1-19 | 6 种对齐（左/右/顶/底/水平居中/垂直居中）每一种对齐后 bbox 值正确（同 x/y / 同中心） | 打印 `graph.getCells().map(c => c.getBBox())` 看数组 | P1-T3.3 |  |  |  |
| P1-20 | 2 种分布（水平等距/垂直等距）中心差值等差；`batchUpdate` 1 步 history（Ctrl+Z 1 步回） | 等差 + Ctrl+Z 验证 | P1-T3.3 |  |  |  |
| P1-21 | 自动布局：7 个模板各跑 TB + LR，各方向 nodes 不重叠、edges 不交叉 | 每个模板 2 个方向截图 | P1-T3.4 |  |  |  |
| P1-22 | 整体布局 1 步 history（Ctrl+Z 1 步回布局前） | — | P1-T3.4 |  |  |  |
| P1-23 | 多页：3 个页签（A/B/C），各画 1/2/3 节点；来回切换 10 次 → 节点计数保持 1/2/3；刷新后仍在；切页 flush 保存（DB 有记录） | SQLite CLI count nodes 校验 | P1-T3.5 |  |  |  |

### 1.4 工具栏 & 属性面板 & 富文本（P1-T4）

| # | 检查点 | 验证方法 | 对应任务 | 结果 | 完成人 | 时间 |
|---|---|---|---|---|---|---|
| P1-24 | 无选中时顶栏 buttons（删除/填充色/置前/连线样式等）disabled；有选中时 enabled | 选中/取消 2 次 | P1-T4.1 |  |  |  |
| P1-25 | 顶栏每个按钮点一次有可观察的画布变化（撤销/重做除外，需先做动作）；全屏切换正常（Fullscreen API） | 逐项触发录屏 | P1-T4.1 |  |  |  |
| P1-26 | 「绘图」面板：改网格颜色（红）→ 即时红；切页面尺寸（16:9）→ 页面边框变宽；竖向/横向切换反转；checkbox 均立即生效 | 每一项操作截图 | P1-T4.2 |  |  |  |
| P1-27 | 富文本：字体/字号/加粗/斜体/下划线/对齐（左中右）6 项全生效，保存后刷新仍保留粗斜样式（innerHTML 存在 data 里） | 1 节点应用所有 6 种 | P1-T4.3 |  |  |  |
| P1-28 | 节点样式：圆角 slider 0~40 正常变化；旋转 45° + 阴影（偏移 + 模糊 + 颜色）+ 线性渐变 + 草图 4 项全生效；每 1 项变化 = 1 步 history（非多步） | 4 种特性叠加 1 节点，Ctrl+Z 每 1 步撤销 1 项 | P1-T4.3 |  |  |  |
| P1-29 | 边样式：线型/线宽/虚线/起点箭头/终点箭头/线色/标签位置 7 项全生效，视觉一致 | 每条设置分别截图 | P1-T4.4 |  |  |  |

### 1.5 高级功能（P1-T5）

| # | 检查点 | 验证方法 | 对应任务 | 结果 | 完成人 | 时间 |
|---|---|---|---|---|---|---|
| P1-30 | 自由画笔：画 1 条连续曲线 → 松手 → 整 1 条 path 作为节点；可移动/改色；Ctrl+Z 1 步撤销整条（非像素级） | 画 3 条曲线测试 | P1-T5.1 |  |  |  |
| P1-31 | 7 个模板点一键套用，edges 无断连（source/target 都存在），nodes 不重叠；zoomToFit 全显示 | 每模板点 1 次截图 | P1-T5.2 |  |  |  |
| P1-32 | AI：输入"电商下单流程" → 5s 内返回 cells；布局后无重叠；无 LLM Key 时返回 mock（不崩）；type guard 拦脏数据（喂非法 JSON → 报 toast 但不崩） | 正常 + 2 异常场景 | P1-T5.3 |  |  |  |
| P1-33 | 导出 PNG：pixelRatio = 2 → Preview 打开不发糊；SVG：用 Safari 独立打开视觉一致；PDF MVP 能出文件（字体稍糊可接受） | 3 种格式分别导出后查看 | P1-T5.4 |  |  |  |
| P1-34 | 旧数据迁移：打开旧 VueFlow 文档 → 形状映射正确（11 种都出现在正确位置）；迁移前原始 JSON 备份到 `wb_diagram.data_backup`；迁移成功 3 次保存后清空备份列 | 准备 3 个典型旧文档测 | P1-T5.5 |  |  |  |
| P1-35 | 自动保存：`cell:added` 后 2s → SQLite data 列更新时间戳变化；Ctrl+S 立即更新；关窗拦截（未保存先保存） | 时间戳比对 + 手动关窗录屏 | P1-T5.5 |  |  |  |

### 1.6 性能与总体体验（Phase 1 底线）

| # | 检查点 | 验证方法 | 对应任务 | 结果 | 完成人 | 时间 |
|---|---|---|---|---|---|---|
| P1-36 | 压力场景：500 节点 + 600 条边（用脚本批量生成），连续 5 分钟编辑（增/删/改色/移动/连线），Chrome Performance 面板 FPS ≥ 30 全程，无主线程 50ms+ Long Task | 录制 Performance 1 次全流程 | All |  |  |  |
| P1-37 | 「快捷键全表」通过：Ctrl+Z / Ctrl+Y / Ctrl+Shift+Z / Ctrl+S / Ctrl+C / Ctrl+V / Delete / Backspace / Esc 全部功能符合预期 | 键盘脚本逐项触发 | All |  |  |  |
| P1-38 | 构建产物 tree-shake 干净：`npm run build` 完成后 `dist/assets/index-*.js` 中无 `vue-flow` 字符串（grep 验证） | grep -i 搜索 | All |  |  |  |

---

## 2. Phase 2 验收（必勾 P0 + P1，P2 可选）

### 2.1 专业形状库 50+（P2-T1 · P0）

| # | 检查点 | 验证方法 | 对应任务 | 结果 | 完成人 | 时间 |
|---|---|---|---|---|---|---|
| P2-01 | 左侧分组手风琴：6+ 分组可展开/折叠；搜索「菱形」→ 所有分组的菱形 shape 均出现；面板宽度可拖拽（200~420px） | 分组点 6 个 + 搜索 1 次 + 拖宽 | P2-T1.1 |  |  |  |
| P2-02 | 17 种 BPMN/流程图符号：每个形状拖拽到画布视觉正确 + label 可编辑 + 可四向连线 | 每个形状拖拽一次 17 张图 | P2-T1.2 |  |  |  |
| P2-03 | UML 类图三段式：类名栏 / 属性栏（+/-/# 前缀）/ 方法栏（参数+返回类型）；双击每栏行编辑；回车新增 / 空 Backspace 删除；高度自适应；6 种关系箭头（继承空心三角、实现虚线三角、聚合空心菱形、组合实心菱形、关联箭头、依赖虚线箭头）视觉均正确 | 画最小类图 2 类 + 6 条关系线 | P2-T1.3 |  |  |  |
| P2-04 | ER：实体(矩形) / 弱实体(双框) / 属性(椭圆) / 多值属性(双椭圆) / 键属性(下划线) / 关系(菱形) / 弱关系(双菱形) / 基数 N:M 标注均正确 | 最小 ER 用户-订单-商品 3 实体 | P2-T1.4 |  |  |  |
| P2-05 | AWS 20 图标矢量清晰（不依赖位图 PNG）；可改 fill color（保留官方原色）；大小可均匀缩放 | 每个拖一次 20 张 + 缩放测试 2 种 | P2-T1.5 |  |  |  |
| P2-06 | 通用/网络 13 形状全部可拖拽 + 命名 + 改色 + 连线；服务器/路由器/交换机/防火墙形状符合常见绘图规范 | 13 种各一次 | P2-T1.6 |  |  |  |
| P2-07 | 统计总形状数 ≥ 50：分组 + 计数脚本（shapeFactory 注册数 sum） | 打印 `Object.keys(shapeRegistry)` length | All P2-T1 |  |  |  |

### 2.2 容器 / 泳道 / 分组 / 图层 / 大纲（P2-T2 · P1）

| # | 检查点 | 验证方法 | 对应任务 | 结果 | 完成人 | 时间 |
|---|---|---|---|---|---|---|
| P2-08 | 容器 parent-child：拖节点 B 进容器 A → A 移动时 B 跟随；A 折叠 → B 隐藏；Ctrl+Z 回折叠动作 | 容器 A + 3 子节点录屏 | P2-T2.1 |  |  |  |
| P2-09 | 横向/纵向泳道：2×2 lanes；节点不能跨 lane 边界（拖拽边界拦截到内侧 1px）；lane 宽可拖拽；lane 名双击编辑 | 横向 + 纵向各测试 1 次 | P2-T2.2 |  |  |  |
| P2-10 | Ctrl+G 分组 → 出现外框 + 组名；移动组 → 子节点整体移动；双击组进编辑模式（其余变淡）；Ctrl+Shift+G 解组组名消失位置保留 | 3 节点 group 全流程 | P2-T2.3 |  |  |  |
| P2-11 | 图层：3 层（A/B/C）；移节点到 B 层 → 关 B 层眼睛 → 节点隐藏；锁 C 层 → C 层节点不可选/编辑；拖拽排序 Layer C 置顶 → 节点 z-index 最高；保存刷新后图层信息仍在 | 3 层每功能点一遍 | P2-T2.4 |  |  |  |
| P2-12 | 大纲面板：泳道 + 2 容器 + 各 3 子节点 → 树层级正确；点击叶子 → 画布选中该 cell 并居中滚动 + 闪烁；搜索"User"过滤出仅匹配 | 画 1 张复杂图 | P2-T2.5 |  |  |  |

### 2.3 版本历史 / 查找替换 / 参考线（P2-T3 · P1）

| # | 检查点 | 验证方法 | 对应任务 | 结果 | 完成人 | 时间 |
|---|---|---|---|---|---|---|
| P2-13 | 版本历史：10 步编辑（每步 >30s 间隔） → 历史列表 10 条 action_label 正确；任一点 → 右侧缩略图预览正确；点「恢复到此版本」→ 自动生成新草稿 `{filename}(恢复到 HH:MM)`（不覆盖原）；每文档上限 100 条（第 101 条删第 1 条） | 10 步连续 + 101 条边界 | P2-T3.1 |  |  |  |
| P2-14 | 查找替换：Ctrl+F 打开浮窗；搜 "process" → 10 节点中 5 个匹配橙闪 1/5；Enter/Shift+Enter 上下跳；替换 1 个 → 仅变 1 条 + 1 步 history；替换全部 → 5 条全变 + 1 步 history；Case sensitive / whole word 两个 checkbox 生效 | 各场景分别测试 | P2-T3.2 |  |  |  |
| P2-15 | Snapline 强化：粉色 2px + 端点"顶对齐/中心对齐/右对齐"文字；灵敏度 10px 正确吸附 | 接近/拉开多次测试 | P2-T3.3 |  |  |  |

### 2.4 导入导出增强（P2-T4 · P2）

| # | 检查点 | 验证方法 | 对应任务 | 结果 | 完成人 | 时间 |
|---|---|---|---|---|---|---|
| P2-16 | PDF：A4 纵向多页 2 页 → 导出 PDF → Preview 打开是 2 页；中文不糊；内容位于 20mm 页边距内不裁切；页脚写 `{filename} · 第 p/n 页 · YYYY-MM-DD HH:MM` | 2 种页面尺寸各导一次 | P2-T4.1 |  |  |  |
| P2-17 | draw.io XML 导入：用 draw.io 官方 2 张 demo 图（基础流程 + 混合复杂）；基础 shape/连线/文字 95% 正确；复杂 stencil 降级为矩形 + 弹窗 warning 列表（N 个形状降级）；orphan edges 不报异常（自动跳过） | 2 张图导入后人工对比截图 | P2-T4.2 |  |  |  |
| P2-18 | draw.io XML 导出：导 7 个模板 → 在 app.diagrams.net（draw.io 官网）打开后 95% 一致；文件大小合理（≤ 原 X6 JSON ×3） | 每个模板导出 + 官网打开截图 | P2-T4.3 |  |  |  |

### 2.5 图片 / 超链接 / Tooltip（P2-T5 · P2）

| # | 检查点 | 验证方法 | 对应任务 | 结果 | 完成人 | 时间 |
|---|---|---|---|---|---|---|
| P2-19 | 图片节点：拖图片形状 → 选本地 JPG（< 5MB）或 粘贴剪贴板 PNG → 立即显示；保存刷新后仍在（dataURL 在 data 中）；比例切换（1:1 / 4:3 / 16:9 / 自适应）裁剪中心正确；size 变化 + 文字名缩放比例同 | JPG + PNG 各 1 次 + 3 比例 | P2-T5.1 |  |  |  |
| P2-20 | 超链接 + Tooltip：节点 href = https://example.com，tooltip = "示例"；hover 500ms → 气泡；按住 Ctrl + 点击节点 → Tauri `shell.open` 调系统浏览器（dev 环境 console 打 "open url" 日志也 OK）；刷新后属性保留 | 1 http + 1 file:// 链接测试 | P2-T5.2 |  |  |  |

---

## 3. 最终交付签核（交付前逐项签）

### 3.1 规格一致性

| # | 检查点 | 结果 | 完成人 | 时间 |
|---|---|---|---|---|
| FINAL-01 | 所有功能严格对齐 spec.md §3 范围；**非目标**项均未实现（未做多人协作/Google Drive/VSDX/MathJax 等，避免 scope creep） |  |  |  |
| FINAL-02 | 代码组织符合 spec.md §4.1 分层：`x6/` composable 与业务解耦；组件仅发事件不直改 graph；无可 export graph 全局单例（必须经 useGraph composable 返回） | 代码走查 |  |  |
| FINAL-03 | 所有 P1 检查点全部 ✅ | 统计 100% |  |  |
| FINAL-04 | P2 中 P0（P2-T1 全部）+ P1（P2-T2 + P2-T3）检查点 100% ✅ |  |  |  |
| FINAL-05 | P2-T4 / P2-T5（P2 可选）完成率 ≥ 80%（若未完成需在 release note 中明确 "计划下一迭代" 项） |  |  |  |

### 3.2 构建 & 质量

| # | 检查点 | 结果 | 完成人 | 时间 |
|---|---|---|---|---|
| FINAL-06 | `npm run build` 构建成功；构建后 bundle 分析（`rollup-plugin-visualizer`）无 VueFlow 残留；X6 总增量体积 ≤ 2MB gzip |  |  |  |
| FINAL-07 | `npm run typecheck` + `npm run lint` 零错误（含新增 x6/ 目录） |  |  |  |
| FINAL-08 | `npm run typecheck` + `npm run lint` 零错误（diagram 相关新表新路由） |  |  |  |
| FINAL-09 | `npm run tauri build`（macOS）打包成功 → .dmg 安装后打开应用，流程图编辑器完整可操作 3 分钟不崩 |  |  |  |
| FINAL-10 | 10 份旧版 VueFlow 文档回归：打开、编辑、保存、刷新、导出均不崩，映射正确 |  |  |  |

### 3.3 签名

| 角色 | 签核名 | 日期 | 备注 |
|---|---|---|---|
| 前端负责人 |  |  |  |
| 后端负责人 |  |  |  |
| 测试/质量 |  |  |  |
| 产品/发起者 |  |  |  |
