# LectoForge v1.1.0 代码审查与性能／体积调优报告

> 审查日期：2026-08-08　范围：`src-ui`（Vue3+TS+Pinia+Vite）／`src-api`（Fastify+Drizzle+better-sqlite3）／`src-tauri`（Tauri2+SidecarManager）
>
> **红线遵守情况**：① `onSend` 信封机制 — 未触碰，所有 Controller 仍 `return` 裸数据；② `defineStore` ID — 零修改；③ better-sqlite3 同步调用 — 全部保持同步，无一处改 `async`。

---

## 零、总览

| 维度 | 核心问题 | 处置 | 状态 |
|---|---|---|---|
| 规范 | `api/types.ts` 1848 行含 135 个死类型 | 脚本化裁剪至 314 行 | 已修复 |
| 规范 | `overviewService` count helper 全 `any` | 收窄为 `SQLiteTable & { userId: AnyColumn }` | 已修复 |
| 规范 | `d3-flextree` 无任何引用 | 卸载 | 已修复 |
| 注释 | SM-2 算法边界／收集箱大小写双契约／导航 match／侧车退避 无说明 | 补 JSDoc | 已修复 |
| 性能 | 笔记列表模板内逐项调用 5 个渲染函数 | computed 预计算 + `v-memo` | 已修复 |
| 性能 | ⌘K 搜索跨三表全量拉取后取前 15 | 每表 `.limit(60)` | 已修复 |
| 体积 | 无 manualChunks，vendor 单块 1.5MB+ | 8 组分包 + visualizer | 已修复 |
| 体积 | 侧车打包含全部 devDependencies | `npm ci --omit=dev` | 已修复 |
| **安全** | **12 个 service 共 0 处 `.limit()`** | 全链路分页 | **已修复** |
| **安全** | **`vault.ts` 路径拼接无根目录校验** | `path.resolve` + 前缀断言 | **已修复** |
| **安全** | **CORS `origin: true` 放行任意来源** | 白名单收口 | **已修复** |

验证结果：`src-api` `tsc --noEmit` **EXIT 0**；`src-ui` `vue-tsc --noEmit` **EXIT 0**。

---

## 一、清理命令与脚本

### 1.1 死类型裁剪（已执行）

`src-ui/src/api/types.ts` 原 1848 行，其中大量类型是三层重构前的遗留契约，无任何 import 引用。手写删除易误伤，故落地为可复跑脚本：

```bash
# 位置：scripts/prune-dead-types.mjs
# 原理：解析 api/types.ts 的所有顶层 export，在 src-ui/src 全量 grep 引用计数，
#      计数为 0 且不被同文件内其他类型引用的，判定为死类型并整块摘除。
node scripts/prune-dead-types.mjs --dry-run   # 先看要删什么（强烈建议）
node scripts/prune-dead-types.mjs             # 实际执行
```

执行结果：**1848 行 → 314 行，删除 135 个死类型，减少 1534 行**。脚本保留在仓库中，后续每次大重构后可复跑。

### 1.2 依赖清理（已执行）

```bash
cd src-ui && npm uninstall d3-flextree
```

**只有这一个是真死依赖**。审计中有两个易误判项，特此记录避免后人重复踩：

| 包 | 初判 | 复核结论 |
|---|---|---|
| `jspdf` / `html2canvas` | grep `from 'jspdf'` 零命中 → 疑似死依赖 | **保留**。`WorkbenchNoteEdit.vue:1462-1508` 用的是动态 `await import('jspdf')`（导出 PDF 冷路径懒加载），静态 grep 抓不到 |
| `src-api` 的 `tsx` / `typescript` / `@types/*` | 源码引用计数 0 | **保留**。devDependencies 本就只在构建期使用，引用计数 0 属正常 |

后端 `dependencies` 全部有实际引用（`@fastify/cors`、`@fastify/multipart`、`@fastify/static`、`axios`、`better-sqlite3`、`cheerio`、`drizzle-orm`、`fastest-levenshtein`、`fastify`、`iconv-lite`），无需清理。

### 1.3 侧车瘦身（已执行）

`scripts/prepare-bin.sh` 原先整包复制 `src-api/node_modules`，把 `typescript`、`tsx`、`@types/*` 一并塞进 `.app`。改为独立 prod 依赖树：

```bash
# prepare-bin.sh 关键改动
npm ci --omit=dev --prefix "$PROD_DIR"      # 只装运行时依赖
# 产物落到 src-api/.prod-modules/node_modules
```

同步改 `src-tauri/tauri.conf.json` 的 `resources` 指向 `.prod-modules/node_modules`，并在 `.gitignore` 中排除该目录。

> ⚠️ **切换 Node 版本后必须重跑**：`cd src-api && npm rebuild better-sqlite3 && bash scripts/prepare-bin.sh`（Node 锁定 24.16.0）。

### 1.4 体积分析（按需执行）

```bash
cd src-ui && ANALYZE=1 npm run build          # 生成并打开 dist/stats.html
cd src-ui && ANALYZE=1 ANALYZE_OPEN=0 npm run build   # CI 场景，不弹浏览器
```

### 1.5 全量校验（提交前必跑）

```bash
# 注意：一律用局部二进制，禁用 npx
cd src-api && ./node_modules/.bin/tsc --noEmit
cd src-ui  && ./node_modules/.bin/vue-tsc --noEmit
cd src-ui  && npm run build     # vite build 才是真打包，vue-tsc 不产出产物
```

---

## 二、完整 `src-ui/vite.config.ts`

已落地文件全文如下（可直接对照校验）：

```ts
import { defineConfig, type PluginOption } from 'vite';
import vue from '@vitejs/plugin-vue';
import path from 'node:path';
import { visualizer } from 'rollup-plugin-visualizer';

/* 是否生成打包体积分析报告。
 * 默认关闭——日常 `npm run build` 不该被浏览器弹窗打断，也不该多花几秒写 stats.html。
 * 需要看体积时执行：ANALYZE=1 npm run build（见 package.json 的 build:analyze 脚本）。 */
const ANALYZE = process.env.ANALYZE === '1';

/**
 * 第三方依赖分包表：`匹配片段 → chunk 名`。
 *
 * 顺序敏感，自上而下取首个命中项。因此**更具体的规则必须排在更宽泛的前面**，
 * 典型如 `vue-chartjs` / `@vue-flow` 都含 "vue" 字样，必须排在裸 `vue` 之前，
 * 否则会被 vendor-vue 抢走，分包退化。
 *
 * 不分包的后果：所有依赖压进单个 vendor.js（本项目约 1.5MB+），
 * 首屏必须等整包下载解析完才能渲染，而其中 markmap / vue-flow / jspdf
 * 只有进到对应页面才会用到——它们跟着路由懒加载走独立 chunk 才是正确形态。
 */
const VENDOR_CHUNKS: Array<[string[], string]> = [
  // —— 必须在裸 vue 规则之前 ——
  [['vue-chartjs', 'chart.js'], 'vendor-chart'],
  [['@vue-flow'], 'vendor-flow'],
  [['lucide-vue-next'], 'vendor-icons'],

  // 核心框架：首屏必需，单独成块以获得最佳长期缓存命中率（版本不变则文件名不变）
  [['/vue/', '/vue-router/', '/@vue/', '/pinia/', 'pinia-plugin-persistedstate'], 'vendor-vue'],

  /* 思维导图：仅 /mindmap 路由使用。markmap 自带整套 d3，并通过 transformer 插件
   * 传递依赖 katex(617KB) 与 yaml(238KB)——这两个必须显式列进来，否则会漏进
   * vendor-misc 拖累首屏（实测未列时 vendor-misc 达 716KB，其中 katex 独占 617KB）。 */
  [
    ['markmap-lib', 'markmap-view', 'markmap-common', 'markmap-html-parser', '/d3-', 'd3-flextree', 'katex', '/yaml/'],
    'vendor-markmap',
  ],

  // Markdown 渲染 + 代码高亮：highlight.js 的语言包体积可观，与编辑器页强绑定
  [['markdown-it', 'highlight.js', 'entities/', 'linkify-it', 'mdurl', 'dompurify'], 'vendor-markdown'],

  /* 导出能力：只在「导出 PDF / 长图」时触发，典型冷路径。
   * canvg / pako 是 jspdf 的传递依赖（SVG 光栅化 / zlib 压缩）；
   * core-js 也必须跟着走——它是 canvg 拖进来的 polyfill，
   * 若留在 vendor-misc 会形成 vendor-misc ⇄ vendor-export 循环引用，
   * Rollup 会报 "Circular chunk" 并退化分包效果。 */
  [['jspdf', 'html2canvas', 'canvg', 'pako', 'core-js'], 'vendor-export'],

  // 音频：番茄钟白噪音专用
  [['howler'], 'vendor-audio'],

  // 通用工具：全站高频复用，合成一块避免碎片化
  [['axios', 'dayjs', '@vueuse'], 'vendor-common'],
];

export default defineConfig({
  plugins: [
    vue(),
    /* 打包体积可视化。gzipSize/brotliSize 会让分析构建变慢，
     * 但没有压缩后体积的报告参考价值很低——反正只在 ANALYZE=1 时才跑。 */
    ANALYZE &&
      (visualizer({
        filename: 'dist/stats.html',
        // CI / 脚本化场景用 ANALYZE_OPEN=0 抑制自动弹浏览器
        open: process.env.ANALYZE_OPEN !== '0',
        gzipSize: true,
        brotliSize: true,
        template: 'treemap',
      }) as PluginOption),
  ].filter(Boolean) as PluginOption[],

  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src'),
      // 共享契约别名：反向引用后端 src-api/src/types（纯类型，仅 import type，bundle 不含 Node 代码）
      '@shared': path.resolve(__dirname, '../src-api/src/types'),
    },
  },

  server: {
    port: 5173,
    // 允许 dev server 读取项目根之外的 src-api 目录（供 @shared 别名解析）
    fs: { allow: ['..'] },
    /* 开发期前端直连本地 Node 后端（与 Tauri 生产同源托管解耦）。
     * /uploads 是用户上传资产（录音 / 图片 / 附件）的同源目录，由后端 @fastify/static
     * 从 <dataDir>/uploads 托管；不代理的话 dev 下 <audio src="/uploads/..."> 会打到
     * vite 自身返回 index.html，表现为「录音存住了但播不出来」。 */
    proxy: {
      '/api': 'http://127.0.0.1:8787',
      '/uploads': 'http://127.0.0.1:8787',
    },
  },

  build: {
    outDir: 'dist',
    emptyOutDir: true,

    /* 产物是本地打进 .app 的，不走 CDN 也就没有网络传输成本，
     * 但 sourcemap 会让 Resources/web 体积翻倍以上，生产关闭。
     * 需要定位线上报错时临时改为 'hidden' 重新构建即可。 */
    sourcemap: false,

    /* 默认 4KB 以下资源会内联成 base64。桌面端是本地读盘，
     * 省下的那次请求毫无价值，反而让 base64 撑大 JS、拖慢解析，故关闭内联。 */
    assetsInlineLimit: 0,

    // 分包后单块理应都在 500KB 以内；仍然告警说明分包表需要补规则
    chunkSizeWarningLimit: 600,

    rollupOptions: {
      output: {
        /**
         * 手动分包：把重型第三方库拆成按需加载的独立 chunk。
         *
         * 只处理 node_modules 内的模块；业务代码保持默认行为，
         * 由 router 的 `() => import()` 自然切成路由级 chunk（已全部懒加载，勿改回静态导入）。
         *
         * @param id 模块的绝对路径 id
         * @returns chunk 名；返回 undefined 交还 Rollup 默认策略
         */
        manualChunks(id: string): string | undefined {
          if (!id.includes('node_modules')) return undefined;
          // 统一分隔符，避免 Windows 下 '\' 导致 '/vue/' 之类的规则失配
          const normalized = id.replace(/\\/g, '/');
          for (const [patterns, chunk] of VENDOR_CHUNKS) {
            if (patterns.some((p) => normalized.includes(p))) return chunk;
          }
          /* 未命中白名单的零散依赖一律返回 undefined，交还 Rollup 默认策略。
           * ⚠️ 不要图省事兜底成 'vendor-misc'：那样会把 canvg / html2canvas 的
           * 各级传递依赖（@babel/runtime、rgbcolor、text-segmentation…）
           * 硬塞进一个与 vendor-export 相互引用的块，Rollup 报
           * "Circular chunk: vendor-misc -> vendor-export -> vendor-misc"，
           * 分包退化且运行期存在初始化顺序风险。实测改回 undefined 后告警消失，
           * 且 Rollup 自动把它们并入真正引用它们的块，总体积反而更小。 */
          return undefined;
        },

        // 产物按类型归档，便于人工核对 Resources/web 里到底装了什么
        chunkFileNames: 'assets/js/[name]-[hash].js',
        entryFileNames: 'assets/js/[name]-[hash].js',
        assetFileNames: 'assets/[ext]/[name]-[hash].[ext]',
      },
    },
  },
});
```

### 配置要点说明

1. **顺序敏感**是这份分包表最大的坑。`vue-chartjs`、`@vue-flow` 都含 `vue` 字样，若排在裸 `vue` 规则之后会被 `vendor-vue` 吞掉。
2. **`vendor-misc` 兜底是反模式**。实测兜底会触发 `Circular chunk: vendor-misc -> vendor-export -> vendor-misc`；返回 `undefined` 让 Rollup 自行归属，总体积反而更小。
3. **katex / yaml 必须显式列入 `vendor-markmap`**。它们是 markmap-lib 的传递依赖，漏列会有 617KB 的 katex 掉进首屏。
4. **`assetsInlineLimit: 0`** 是桌面端特化：本地读盘无网络往返成本，base64 内联纯属负收益。

---

## 三、高风险修改清单

以下三类改动触及数据读取边界与文件系统边界，**回归验证优先级最高**。

### 【高危 1】全站 `list` 接口零分页 — 数据库

**问题**：全项目 12 个 service 的所有列表查询 **`.limit()` 出现次数为 0**。better-sqlite3 是同步驱动，一次 `SELECT *` 全表扫描会**阻塞整个 Node 事件循环**，笔记量上万后表现为「点一下列表，整个 App 卡死数秒」。

**改法**：入参扩展、**出参形态不变**（仍返回裸数组），从而完全不触碰 `onSend` 信封机制。

新增两个文件，严格遵守 `types/` 不含运行时值的架构约束：

```ts
// src-api/src/types/pagination.ts —— 纯类型
export interface PageQuery {
  page?: number;
  pageSize?: number;
  limit?: number;
  offset?: number;
}
export interface ResolvedPage {
  limit: number;
  offset: number;
}
```

```ts
// src-api/src/lib/pagination.ts —— 运行时归一化
export const DEFAULT_PAGE_SIZE = 200;  // 安全网上限，非强制分页
export const MAX_PAGE_SIZE = 500;      // 单页硬顶，钳制恶意入参

export function resolvePage(q: PageQuery | undefined, fallback = DEFAULT_PAGE_SIZE): ResolvedPage;
export function pickPage(raw: unknown): PageQuery;  // 从 Fastify query 抽取并转 number
```

**为什么默认 200 而不是 20**：现有前端多处依赖「一次拿全量再本地筛选」的隐式契约。若直接默认 20，前端会静默丢数据且难以察觉。200 作为安全网既能挡住失控查询，又对绝大多数用户（笔记 < 200 条）完全无感。待前端逐页面接入 `page/pageSize` 后可再下调。

`page/pageSize` 优先级高于 `limit/offset`，两套入参并存以兼容不同调用方。

**受影响文件（11 个）**：

| 层 | 文件 | 改动 |
|---|---|---|
| types | `capture.ts` `note.ts` `inbox.ts` `story.ts` `reviews.ts` | List*Query 改为 `extends PageQuery` |
| service | `captureService.ts` `noteService.ts` `storyService.ts` `reviewsService.ts` | 各 list 方法加 `resolvePage(q)` + `.limit().offset()` |
| service | `inboxService.ts` | 同上；另 `listByStatus(status, page?)` 新增分页形参；`conds: any[]` → `SQL[]` |
| controller | `captureController.ts` `noteController.ts` `storyController.ts` `reviewsController.ts` | query 对象加 `...pickPage(req.query)` |
| controller | `inboxController.ts` | `ListQuery`/`StatusQuery` 改 `extends PageQuery`，`listByStatus` 传 `(q.status, q)` |

典型改动形态（`noteService.listNotes`）：

```ts
const { limit, offset } = resolvePage(q);
const rows = db.select().from(wbNote)
  .where(and(...conds))
  .orderBy(desc(wbNote.updatedAt))
  .limit(limit).offset(offset)      // ← 唯一新增
  .all();
```

**回归重点**：
- 笔记 / 收集箱 / 故事 / 复习卡四个列表页，确认数据条数与改动前一致（< 200 条时应完全相同）。
- 收集箱按状态筛选（INBOX / ARCHIVED / TRASHED）四个页签均需点一遍——`listByStatus` 签名变了。
- 传 `?pageSize=99999` 应被钳到 500，不应报错。
- 传 `?page=0` / 负数 / 非数字，应回落到默认值而非抛异常。

### 【高危 2】⌘K 全局搜索三表全量扫描 — 数据库

**问题**：`searchService.ts` 对笔记 / 收集箱 / 故事三张表分别 `SELECT` **全部匹配行**，在内存里合并排序后只取前 15 条返回。命中量大时同步查询直接卡死主线程，是全项目最典型的性能反模式。

**改法**：

```ts
const PER_TABLE_LIMIT = 60;   // = 4 × MAX_RESULTS(15)

// 三个子查询各自加：
.orderBy(desc(wbNote.updatedAt))
.limit(PER_TABLE_LIMIT)
```

**为什么 60 与全量排序等价**：最终只取 15 条。即便某一张表垄断全部结果，也只需要该表贡献 15 条；取 4 倍冗余（60）后再合并排序，数学上不可能挤掉任何本该入选的行。

**回归重点**：⌘K 搜高频词（如「学习」），确认结果与改动前一致且明显变快。

### 【高危 3】`vault.ts` 路径穿越 — 文件系统安全

**问题**：文件读写路径由用户可控输入直接拼接，缺少根目录归属校验。构造 `../../../../etc/passwd` 之类的相对路径可越出数据目录读写任意文件。

**改法**：所有路径先 `path.resolve` 归一化，再断言必须落在允许的根目录下：

```ts
const resolved = path.resolve(root, userInput);
if (!resolved.startsWith(root + path.sep) && resolved !== root) {
  throw new Error('path traversal detected');
}
```

**回归重点**：
- 正常上传录音 / 图片 / 附件，`/uploads/*` 静态托管可正常访问（dev 下走 vite proxy）。
- 资料库导入 / 导出、笔记附件读取全流程。
- 文件名含中文、空格、Emoji 时不应被误判拦截。

### 【中危 4】CORS `origin: true` — 网络安全

`src-api/src/index.ts` 原配置 `origin: true` 等价于反射任意 `Origin` 并放行。桌面端侧车监听 `127.0.0.1:8787`，本机任意网页均可跨域读取用户全部笔记数据。已收窄为白名单（Tauri 来源 + dev 的 `localhost:5173`）。

**回归重点**：`npm run tauri:dev` 与打包后的 `.app` 双形态都要验证接口不被 CORS 拦截——两者 Origin 不同。

### 已评估但**刻意不改**的项

| 项 | 位置 | 不改的理由 |
|---|---|---|
| `ref<any[]>` ×2 | `FlowchartEditor.vue:173-174` | 规避 vue-flow 的 TS2589「类型实例化过深」，是有注释依据的正当豁免，强改会导致编译失败 |
| `as any` ×2 | `memory-palace-store.ts` | 复核为注释文字，非真实 any |
| `as any` 若干 | `aiInsightService.ts` `migrationService.ts` | Drizzle 泛型表操作的常规写法，收窄成本高、收益低 |
| `/associate` 字段名 Bug | `aiEmbeddingService.ts` | 读 `e.entity_type`，Drizzle 实返 camelCase `entityType` → 关联项标题恒空。**这是真 Bug**，但属功能缺陷而非本次调优范围，需单开 `fix(ai):` 提交 + 专项回归，避免污染本次快照 diff |

---

## 四、执行清单（Git 提交顺序）

### ⚠️ 前置：工作区含 3 组「非本次审查」的历史未提交改动

`git status` 中混入了更早会话遗留的改动，**必须先隔离**，否则本次调优的提交会被污染、失去回滚价值：

| 组 | 文件 | 性质 |
|---|---|---|
| **A. 间隔复习功能** | `src-api/src/services/reviewService.ts`（单数，+390 行）、`controllers/reviewController.ts`、`routes/review.ts`、`types/review.ts`、`db/index.ts`、`db/schema.ts` | 独立功能开发，与本次无关。注意与本次改的 `reviewsService.ts`（**复数**）是两个文件 |
| **B. 应用图标重生成** | `icon-source.png` + `src-tauri/icons/**`（约 50 个二进制） | 图标资源更新 |
| **C. 引导页素材** | `src-ui/public/onboarding/`（未跟踪）、`Mock数据清零改造报告.md`（未跟踪）、删除的 `代码结构迁移与重构方案.md` / `重构执行清单.md` | 引导设置功能与文档整理的收尾 |

**建议**：先把 A / B / C 各自单独提交（或 `git stash` 挂起），再执行下面的本次调优提交序列。

顺带清理两个残留备份文件：

```bash
rm -f icon-source.png.bak src-tauri/icons/icon.png.bak
```

### 本次调优提交序列（7 个提交，严格按序）

顺序原则：**低风险在前、高风险在后**。这样一旦线上出问题，`git bisect` 能快速定位；且高危改动单独成提交，可精准 revert 而不牵连其他优化。

---

**① 基础设施：新增分页工具**

```bash
git add src-api/src/types/pagination.ts src-api/src/lib/pagination.ts
git commit -m "feat(api): 新增分页契约与归一化工具

- types/pagination.ts: PageQuery/ResolvedPage 纯类型，遵守 types 层无运行时值约束
- lib/pagination.ts: resolvePage/pickPage + DEFAULT_PAGE_SIZE=200/MAX_PAGE_SIZE=500
- page/pageSize 优先级高于 limit/offset，两套入参并存兼容不同调用方"
```

> 纯新增文件，零风险。先落地是为了让后续提交都能编译通过。

---

**② 死代码与依赖清理**

```bash
git add scripts/prune-dead-types.mjs src-ui/src/api/types.ts src-ui/package.json src-ui/package-lock.json
git commit -m "chore: 清理死类型与死依赖

- 新增 scripts/prune-dead-types.mjs（支持 --dry-run），可复跑
- api/types.ts 1848→314 行，删除 135 个零引用死类型
- 卸载 d3-flextree（markmap 不引用）
- 保留 jspdf/html2canvas：WorkbenchNoteEdit.vue 有动态 import"
```

---

**③ 注释补全**

```bash
git add src-api/src/services/sm2.ts src-ui/src/components/layout/DesktopTopNav.vue src-tauri/src/lib.rs
git commit -m "docs: 为核心逻辑补充 JSDoc

- sm2.ts: SM-2 算法各参数边界与 EF 下限来由
- DesktopTopNav.vue: navItems 的 startsWith 匹配规则与顺序敏感性
- lib.rs: SidecarManager 指数退避重启策略与端口一致性约束"
```

> `inboxService.ts` 的双契约注释与其分页改动在同一文件，合并进提交 ⑥。

---

**④ 打包体积优化**

```bash
git add src-ui/vite.config.ts scripts/prepare-bin.sh src-tauri/tauri.conf.json .gitignore
git commit -m "perf(build): 手动分包与侧车瘦身

- vite.config.ts: 8 组 manualChunks + ANALYZE=1 体积可视化
- 关闭 sourcemap 与 assetsInlineLimit（桌面端本地读盘特化）
- prepare-bin.sh: npm ci --omit=dev，产物落 .prod-modules/node_modules
- tauri.conf.json resources 同步指向新目录
- .gitignore 排除 .prod-modules 与 types.ts.bak"
```

**验证卡点**：本提交后必须跑 `cd src-ui && npm run build`，确认无 `Circular chunk` 告警、无单块超 600KB 告警。

---

**⑤ 前端渲染优化**

```bash
git add src-ui/src/views/WorkbenchNotes.vue
git commit -m "perf(ui): 笔记列表预计算视图模型 + v-memo

- 新增 NoteCardVm 接口与 noteCards computed，模板内消除
  srsOf/preview/tagsOf/masteryColor/srsHint 5 个渲染期函数调用
- grid/list 两处 v-for 加 v-memo 双防层
- 动作回调改传 c.raw，保持原有数据流"
```

**验证卡点**：笔记列表 grid / list 双视图切换，卡片内容、标签、掌握度配色、SRS 提示均需与改动前一致。

---

**⑥ 【高危】数据库分页改造**

```bash
git add src-api/src/types/capture.ts src-api/src/types/note.ts src-api/src/types/inbox.ts \
        src-api/src/types/story.ts src-api/src/types/reviews.ts \
        src-api/src/services/captureService.ts src-api/src/services/noteService.ts \
        src-api/src/services/storyService.ts src-api/src/services/reviewsService.ts \
        src-api/src/services/inboxService.ts src-api/src/services/searchService.ts \
        src-api/src/services/overviewService.ts \
        src-api/src/controllers/captureController.ts src-api/src/controllers/noteController.ts \
        src-api/src/controllers/storyController.ts src-api/src/controllers/reviewsController.ts \
        src-api/src/controllers/inboxController.ts
git commit -m "perf(api): 全站 list 接口接入分页，消除全表扫描

- 12 个 service 原本 0 处 .limit()，同步驱动下大数据量会阻塞事件循环
- 入参扩展、出参仍为裸数组，不触碰 onSend 信封机制
- searchService 三表各加 PER_TABLE_LIMIT=60（=4×MAX_RESULTS，与全量排序等价）
- inboxService listByStatus 新增分页形参；conds 由 any[] 收窄为 SQL[]
- inboxService 补双契约注释（小写 VO ↔ toStatusDb ↔ DB 大写）
- overviewService count helper 由 any 收窄为 SQLiteTable & { userId: AnyColumn }"
```

**验证卡点**（务必逐条走查）：
1. 笔记 / 收集箱 / 故事 / 复习卡四个列表页数据条数与改前一致
2. 收集箱 INBOX / ARCHIVED / TRASHED 三个页签逐一点开
3. ⌘K 搜高频词，结果一致且更快
4. 异常入参 `?pageSize=99999` / `?page=0` / `?page=abc` 均不报错

---

**⑦ 【高危】安全加固**

```bash
git add src-api/src/lib/vault.ts src-api/src/index.ts
git commit -m "fix(security): 路径穿越防护与 CORS 收口

- vault.ts: 所有路径 path.resolve 归一化后断言必须落在允许根目录内
- index.ts: CORS 由 origin:true（反射任意来源）收窄为白名单

侧车监听 127.0.0.1:8787，origin:true 意味着本机任意网页可跨域读取全部笔记数据"
```

**验证卡点**：
1. 上传录音 / 图片 / 附件，`/uploads/*` 可正常访问
2. 资料库导入导出全流程
3. 含中文 / 空格 / Emoji 的文件名不被误拦
4. **`tauri:dev` 与打包后 `.app` 双形态都要验 CORS**（两者 Origin 不同）

---

### 提交后的收尾

```bash
# 全量校验
cd src-api && ./node_modules/.bin/tsc --noEmit      # 期望 EXIT 0
cd src-ui  && ./node_modules/.bin/vue-tsc --noEmit  # 期望 EXIT 0
cd src-ui  && npm run build                          # 无 Circular chunk / 超限告警

# 真机验证
npm run tauri:dev       # dev 形态
npm run tauri:build     # 打包形态，重点验 CORS 与侧车启动
```

> **推送策略**：本报告只规划到本地 `commit`。`git push` 需你逐次确认后再执行。

---

## 五、当前验证状态

| 项 | 结果 |
|---|---|
| `src-api` `tsc --noEmit` | ✅ EXIT 0 |
| `src-ui` `vue-tsc --noEmit` | ✅ EXIT 0 |
| 分页边界用例 | ✅ 12/12 通过（默认值 / 钳制 / 负数 / 非数字 / page 与 limit 优先级） |
| `onSend` 信封 | ✅ 未触碰，Controller 全部 `return` 裸数据 |
| `defineStore` ID | ✅ 零修改 |
| better-sqlite3 同步性 | ✅ 无一处改 async |

### 遗留待办

> 三项遗留已于 2026-08-08 当晚全部整改完成，详见 **[第六节](#六遗留待办整改已完成)**。

1. ~~`aiEmbeddingService.ts` `/associate` 字段名 Bug（`entity_type` → `entityType`）~~ ✅ 已修复（`fix(ai):`）
2. ~~前端接入分页 UI~~ ✅ 已完成（滚动加载 + 统计聚合端点）
3. ~~虚拟滚动~~ ✅ 已引入 `useVirtualList`

---

## 六、遗留待办整改（已完成）

> 整改时间：2026-08-08 当晚。代码改动未提交（沿用「大功能改造后自动 commit、推送逐次确认」纪律，等待你确认 A/B/C 三组历史未提交改动隔离方案后再统一提交）。

### 6.1 `aiEmbeddingService.ts` `/associate` 字段名 Bug（fix(ai):）

- **根因**：原写法 `db.select().from(wbEmbedding).all() as any[]`，`as any[]` 遮蔽了类型，使 `e.entity_type` 这种**不存在的字段**在编译期不可见。而 Drizzle 实返 camelCase `entityType`/`entityId`，导致关联项标题恒为「(已删除)」。
- **修复**：去掉 `as any[]`，只 `select` 需要的四列并保持强类型：
  ```ts
  const all = db
    .select({ entityType: wbEmbedding.entityType, entityId: wbEmbedding.entityId, model: wbEmbedding.model, vector: wbEmbedding.vector })
    .from(wbEmbedding)
    .all();
  // 后继 map 直接读 e.entityType / e.entityId
  ```
- **回归防线**：写回 `e.entity_type` 时 `tsc` 报 `TS2551: Property 'entity_type' does not exist... Did you mean 'entityType'?`，确认「字段名错误」此后会被编译期直接拦截，无法再悄悄混进。
- **影响文件**：`src-api/src/services/aiEmbeddingService.ts`

### 6.2 前端接入分页 UI + 统计聚合

后端 `/notes` 早已支持 `page/pageSize`，但前端一直全量拉取、且 `DEFAULT_PAGE_SIZE=200` 只是兜底。本次补齐前端分页消费链路：

- **后端**：新增 `GET /notes/stats`（`noteController.stats` → `noteService.getNoteStats`）走 SQL 聚合直出 `{ total, due, unreviewed, avgMastery }`；列表与统计**共用** `buildNoteConds` / `parseListQuery` 单一落点，杜绝筛选口径漂移。列表默认页大小由 `DEFAULT_PAGE_SIZE` 细化为 `NOTES_PAGE_SIZE = 30`（真实分页粒度），`DEFAULT_PAGE_SIZE` 保留作未迁移模块的兜底安全网。
- **前端 store**（`src-ui/src/store/note-store.ts`）：
  - `fetchNotes` 首屏用 `Promise.all([listNotes(page:1), getNoteStats(query)])` 并发拉列表+统计；
  - 新增 `loadMore()`：三重防护——并发闸门（`loading||loadingMore||!hasMore`）、id 去重（`Set`，offset 分页并发删改下防 `:key` 冲突）、空返回收敛（`stats.total = notes.length`）；
  - `hasMore = computed(() => notes.length < stats.total)`（用「是否装满」而非「是否等于总数」判定，避免整数倍边界多发空请求）；
  - `removeNote` 乐观递减 `stats.total` + 成功后 `refreshStats()` 异步校准；
  - 清理零引用的 `isNoteDue`。
- **验证**：用 `LECTOFORGE_DB` 重定向测试库 + Node 24（better-sqlite3 按此版本编译）跑 12 条断言（统计四数 / 筛选跟随 / 标签空集短路 / 默认页 30 / 分页边界 / 越界空 / `MAX_PAGE_SIZE` 钳制），**12/12 PASS**。

### 6.3 虚拟滚动（useVirtualList）

- **行模板抽出** `src-ui/src/components/NoteRow.vue`：把约 30 行的紧凑行标记独立成组件，`grid` 与 `list` 两视图、虚拟/非虚拟两分支**共用同一份**，杜绝复制漂移；`NoteCardVm` 类型一并抽到该组件顶部导出。
- **列表视图**（`WorkbenchNotes.vue`）：
  - `ROW_HEIGHT = 61`（推导：标题 13px×1.5 + 间距 2 + 摘要 11px×1.5 + padding 22 + border 1）；JS 作为行高唯一真源，经根元素 `--note-row-h` 下发给 CSS，虚拟位移与渲染高度永不失配。
  - `VIRTUAL_THRESHOLD = 120`：低于此值保持整页滚动 + 原生 `v-for`（避免嵌套滚动条代价）；高于则用 `useVirtualList`（`itemHeight: 61, overscan: 6`），容器自身成为固定高度滚动视口（`.notes-list--virtual { height: calc(100vh - 360px) }`）。
  - 阈值判定取「筛选后总数」而非「已加载条数」，防止滚到第 4 页时半途切换滚动模式导致视口瞬移。
- **无限滚动**：`useInfiniteScroll`（而非手写 IntersectionObserver，因其回调后 `nextTick` 自复检能补满视口）挂两套、用 `canLoadMore` 互斥：非虚拟观察 `window`，虚拟观察 `containerProps.ref`。
- **网格视图**：行高不定不进虚拟，改用 `content-visibility: auto` + `contain-intrinsic-size` 渐进增强，千卡不卡。
- **底部状态条**：`notes-foot` 实时反馈「加载中 / 已到底（共 N 则）/ 向下滚动加载更多」。
- **验证**：`src-ui` `vue-tsc --noEmit` **EXIT 0**；`src-api` `tsc --noEmit` **EXIT 0**。

### 6.4 整改后验证总览

| 项 | 结果 |
|---|---|
| `src-api` `tsc --noEmit` | ✅ EXIT 0 |
| `src-ui` `vue-tsc --noEmit` | ✅ EXIT 0 |
| `/associate` 字段名回归防线（TS2551） | ✅ 写回旧字段名被编译拦截 |
| 分页统计 12 条边界用例 | ✅ 12/12 PASS |
| `onSend` 信封 / `defineStore` ID / better-sqlite3 同步性 | ✅ 三项红线零触碰 |
| 新增组件 `NoteRow.vue` 类型与样式隔离 | ✅ scoped 样式 + 全局 action 样式复用 |
