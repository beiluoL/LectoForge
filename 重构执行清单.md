# LectoForge 渐进式安全重构 — 原子化步骤清单

> 基线提交：`40bf91d refactor: 产品全局更名 KnowFlow → LectoForge`
> 生成时间：2026-08-08　｜　状态：**待确认，尚未执行任何代码改动**
> 约束依据：用户下发的《8 条强制重构原则》

---

## 一、现状体检（重构前基线，全部为实测数据）

### 1.1 后端 `src-api`（8846 行 TS）

| 指标 | 实测 | 对照原则 |
|---|---|---|
| 路由文件 | 18 个 | — |
| HTTP 端点 | 119 个 | — |
| `services/` 目录 | **仅有 `sm2.ts`（61 行）** | 原则二：几乎为空 |
| `controllers/` 目录 | **不存在** | 原则二：缺失 |
| 路由层含 Drizzle 查询的文件 | **15 / 18** | ❌ 原则二 |
| 路由层含第三方 HTTP 调用 | `ai.ts`（axios）、`library.ts`（cheerio 抓取） | ❌ 原则二 |
| `any` / `@ts-ignore` | **200 处**（集中在 routes） | ❌ 原则五 |
| 响应信封 | `index.ts` 的 `onSend` 钩子统一包 `{code:200,data}` | ✅ 原则一红线，**不得改动** |

路由层数据库调用密度（按需要下沉的工作量排序）：

```
ai.ts        1130行  db:14   ← 含 axios 第三方调用，最复杂
inbox.ts      897行  db:11   ← 含 multipart 上传
review.ts     379行  db: 9   ← 触碰 sm2，红线最紧
pomodoro.ts   321行  db: 2   ← 主要是 fs
mindmap.ts    302行  db: 0   ← 纯 JSON 文件存储
notes.ts      307行  db:10
migration.ts  278行  db: 2   ← 一次性迁移，特殊
reviews.ts    276行  db:11
palaces.ts    203行  db:11
library.ts    197行  db: 0   ← 纯磁盘 Markdown + cheerio
recall.ts     160行  db: 6
config.ts     131行  db: 0
captures.ts   122行  db: 8   ← ★ 最适合作试点：纯 CRUD、无外部依赖
search.ts     120行  db: 1
stories.ts    117行  db: 7
dashboard.ts  112行  db: 1
overview.ts    72行  db: 3
categories.ts  18行  db: 3
```

### 1.2 前端 `src-ui`（31828 行）

| 检查项 | 实测结果 | 对照原则 |
|---|---|---|
| `axios` / `fetch` 直接调用 | 仅 `api/request.ts`、`utils/connection.ts` | ✅ **原则三已达标** |
| Pinia 目录 | `store/` 6 个 + `stores/` 2 个**并存** | ❌ 原则七 |
| Store 命名 | 全部 camelCase（`appStore.ts`…） | ❌ 原则七（要求 kebab-case） |
| `store/index.ts` 桶导出 | **不存在** | ❌ 原则七 |
| `constants/` 目录 | **不存在** | ❌ 原则四 |
| 状态机/颜色/文案硬编码 | `archive/WorkbenchCapture.vue` 内联 `INBOX/PROCESSED/ARCHIVED` 三份映射（label / color / tabs）；`InboxList.vue`、`AiAssociatePanel.vue`、`AiInsights.vue` 各自实现 `typeLabel()` | ❌ 原则四 |
| 时间格式化 | `utils/time.ts` 已有 `fromNow`/`formatDateTime`，但**只有 2 个文件引用**；`WorkbenchNoteEdit.vue`、`WorkbenchRecall.vue` 各自私写 `formatTime()` | ❌ 原则四 |
| Markdown 渲染器 | `lib/markdown.ts`（461 行）已是**唯一实现**，`views/DocLibrary/markdown.ts` 是 21 行再导出 shim | 🟡 **原则八已完成 90%** |
| 残余违规（原则八） | `Review/FlashCard.vue` 绕道 `@/views/DocLibrary/markdown` 引入 | ❌ 需改为 `@/lib/markdown` |
| `any` / `@ts-ignore` | 实测基线 26 处（`FlowchartEditor.vue` 10 处最多），Phase 3.6 已清零至 **2 处**（均为 VueFlow `nodes`/`edges` ref，TS2589 使然，已注释说明） | 🟢 **原则五已完成**（仅 2 处库强制保留） |

> 附带发现：`views/DocLibrary/useDocStore.ts`、`views/MindMap/useMindMapStore.ts` 名字带 Store 但**不是** `defineStore`，是普通 composable，不纳入原则七的迁移范围。

---

## 二、⚠️ 三个必须你拍板的原则冲突点

这三处如果我自行决定，一定会踩到红线或过度设计，因此先摆出来。

### 冲突 A：原则二的「schema 参数校验」 vs 原则一的「行为零退化」

现状校验是手写的，例如 `captures.ts`：

```ts
if (!b.title) return reply.code(400).send({ message: 'title required' });
```

若改用 Fastify JSON Schema 校验，会被 `index.ts` 的 `setErrorHandler` 拦截，响应体变成 `{ code: 400, message: 'invalid request' }` —— **与重构前不一致，直接违反原则一红线**。

| 方案 | 说明 | 代价 |
|---|---|---|
| **A1（推荐）** | 第一阶段只做分层搬运，手写校验原样搬进 Controller；schema 校验单列为「阶段二」，届时统一评估错误契约 | 100% 零退化，但原则二的「schema 校验」暂缓 |
| A2 | 现在就加 Fastify JSON Schema，并同步改 `setErrorHandler` 让 400 沿用原文案 | 需为 119 个端点逐一比对错误文案，工作量与出错概率都高 |
| A3 | 引入 TypeBox/Zod | 新增依赖 + 全量错误契约变更，风险最高 |

### 冲突 B：原则三「Controller 不能直接调用 Service 具体实例」的落地强度

Node/Fastify 生态里做完整 IoC 容器（tsyringe / awilix）需要装饰器 + `reflect-metadata`，改动面远超重构本身。

| 方案 | 说明 |
|---|---|
| **B1（推荐）** | 手工构造函数注入：Service 先定义 `interface ICapturesService`，Controller 写成工厂 `createCapturesController(svc: ICapturesService)`，路由文件充当 composition root 完成组装。零新依赖，满足「面向接口 + 可替换 + 可 mock」 |
| B2 | 引入 awilix 做容器自动装配 |

### 冲突 C：原则五「类型必须从后端 Schema 生成」的落地路径

现状是前端 `api/types.ts`（1868 行手写）与后端 `src/types.ts`（188 行手写）**两套独立定义**，且前端注释仍写着「与 com.lectoforge.vo 对齐」（Java 时代遗留）。

| 方案 | 说明 |
|---|---|
| **C1（推荐）** | 新建 `shared/types/` 顶层目录，后端从 Drizzle schema 用 `$inferSelect` 推导实体类型并导出 VO/Payload；前后端 tsconfig 各加一条 path 别名指过去。逐模块搬，不搞一次性大迁移 |
| C2 | 保持两套，仅在重构模块时手工对齐字段 |
| C3 | 引入 openapi/ts-rest 代码生成链路 |

---

## 三、原子化重构步骤清单

**总原则**：每批 = 一次 `git commit`；每批结束都有可执行的验证指令；任一批验证不过则 `git reset --hard` 回退，绝不带病推进。

### Phase 0 — 建立安全网（**不改任何业务代码**）

| # | 动作 | 产物 |
|---|---|---|
| 0.1 | 新建 `scripts/api-snapshot.sh`：curl 打 ~20 个代表性只读端点，输出落 `.refactor-baseline/*.json` | 契约快照脚本 |
| 0.2 | 在基线提交上跑一次，生成 `before/` 快照 | 重构前响应体基线 |
| 0.3 | 建空目录 + `.gitkeep`：`src-api/src/controllers/`、`src-ui/src/constants/`（`src-api/src/services`、`src-ui/src/composables` 已存在） | 目标骨架 |

- 验证：`bash scripts/api-snapshot.sh before` 全部返回 200
- 提交：`git add . && git commit -m "chore(refactor): 建立契约快照安全网与分层目录骨架"`

> 此后每批结束都跑 `bash scripts/api-snapshot.sh after && diff -r .refactor-baseline/before .refactor-baseline/after`，**diff 必须为空** —— 这就是原则一的自动化验证点。

---

### Phase 1 — 后端分层范式试点：`captures`（★ 关键批次）

选它的理由：122 行、纯 CRUD、无 fs/无第三方调用、无 SM-2 参与，是建立范式的最小完备样本。

| # | 动作 |
|---|---|
| 1.1 | 新建 `services/captures.service.ts`：定义 `ICapturesService` 接口 + `createCapturesService(deps)` 实现，把 8 处 Drizzle 查询与 `toVO()` 全部搬入 |
| 1.2 | 新建 `controllers/captures.controller.ts`：`createCapturesController(svc: ICapturesService)`，只做「取参 → 调 service → 定 HTTP 状态码」，保留 404/400 原文案 |
| 1.3 | 改写 `routes/captures.ts`：只剩 6 行 `app.get/post/put/delete` 绑定，**零 Drizzle import** |
| 1.4 | 顺手消除该模块 15 处 `any`（`req.query as any` → 明确 Params/Query/Body 类型） |

- 验证：
  ```bash
  cd src-api && ./node_modules/.bin/tsc --noEmit -p tsconfig.json   # 必须 0 error
  npm run dev:api                                                   # 另开终端
  curl -s http://127.0.0.1:8787/api/health | head -c 200
  curl -s "http://127.0.0.1:8787/api/workbench/captures" | head -c 400
  bash scripts/api-snapshot.sh after && diff -r .refactor-baseline/before .refactor-baseline/after
  ```
- 提交：`git add . && git commit -m "refactor(api): captures 下沉 Controller/Service 三层，建立分层范式"`

**这一批完成后我会停下来，请你确认范式（接口粒度、命名、注入写法）是否满意，再批量套用。**

---

### Phase 2 — 后端小模块批量套用（低风险，逐个提交）

按「体量小 → 依赖少」排序，**每个模块单独一次 commit**：

| 批次 | 模块 | 端点数量级 | 备注 |
|---|---|---|---|
| 2.1 | `categories` | 18 行 | 最小热身 |
| 2.2 | `overview` | 72 行 | 纯聚合读 |
| 2.3 | `dashboard` | 112 行 | 纯聚合读 |
| 2.4 | `search` | 120 行 | 跨三表模糊检索 |
| 2.5 | `stories` | 117 行 | 简单 CRUD |
| 2.6 | `notes` | 307 行 | 康奈尔笔记 CRUD |
| 2.7 | `recall` | 160 行 | 主动回忆会话 |
| 2.8 | `palaces` | 203 行 | 宫殿 + loci 双表 |

- 每批验证同 Phase 1（tsc + 快照 diff）
- 提交模板：`refactor(api): <模块> 下沉 Controller/Service`

---

### Phase 3 — 前端归一化（与后端解耦，可并行插入）

| 批次 | 动作 | 对应原则 |
|---|---|---|
| 3.1 | **Markdown 收口**：`Review/FlashCard.vue` 改引 `@/lib/markdown`；`DocLibrary/EditorArea.vue`、`useTOC.ts` 的 `./markdown` 改为 `@/lib/markdown`；删除 21 行 shim `views/DocLibrary/markdown.ts` | 八 |
| 3.2 | **Pinia 归一**：`stores/inboxStore.ts` → `store/inbox-store.ts`、`stores/searchStore.ts` → `store/search-store.ts`；现有 6 个 store 一并改 kebab-case；新建 `store/index.ts` 桶导出；更新 8 处 import；删除 `stores/` 目录 | 七 |
| 3.3 | **常量收敛**：新建 `constants/capture.ts`（`CAPTURE_STATUS` / `CAPTURE_STATUS_LABEL` / `CAPTURE_STATUS_COLOR` / `CAPTURE_TABS`）、`constants/inbox.ts`（`INBOX_TYPE_LABEL`）、`constants/entity.ts`（AI 关联的 `ENTITY_TYPE_LABEL`）；改造 `WorkbenchCapture.vue`、`InboxList.vue`、`AiAssociatePanel.vue`、`AiInsights.vue` 四处 | 四 |
| 3.4 | **时间收敛**：`utils/time.ts` 升格为 `lib/date.ts`（补 `formatClock`/`formatDuration`），删除 `WorkbenchNoteEdit.vue`、`WorkbenchRecall.vue` 的私有 `formatTime()`，全量改引 | 四 |
| 3.5 | **类型收敛**：按冲突 C 选定方案落地 `shared/types/`（若选 C1）。**实际完成 6/16**：`WorkbenchOverview` + 五个 Payload 已指向 `@shared`，并补上 `api/types.ts` 文件头「在用 / 遗留」分界标记；6 个读模型 VO 因 34 处契约差异（含 6 处字面量联合退化）暂缓，规格见方案 §6.2.1，留给独立的「契约对齐 PR」 | 五 |
| 3.6 | **`any` 清零**：实际基线 26 处（非 23），分三批消除。`FlowchartEditor.vue` 10 处优先。**结果：前端全局仅剩 2 处 `any`**，均为 `FlowchartEditor.vue` 的 `nodes`/`edges` ref——已实证在 `Node[]`/`Edge[]` 上做 `.map`/展开会触发 TS2589（vue-flow 类型实例化链路过深），故按「保留但收窄到最小范围」保留 `any[]` 并在注释说明；其余 24 处全部收窄为具体类型（catch 用 `instanceof Error`、第三方回调用库自带类型如 `StateInline`/`TooltipItem`/`Component`/`Partial<IMarkmapOptions>`、`MindMapNode`/`MindMapEdge` 接口、`setEditorEl` 用 `Element \| ComponentPublicInstance \| null` 等）。三批提交：`950eb17` / `e81e2bd` / `48a6bf7` | 五 |

- 每批验证：
  ```bash
  cd src-ui && ./node_modules/.bin/vue-tsc --noEmit    # 必须 0 error
  npm run build:web                                    # vite build 才是真打包
  ```
- 提交模板：`refactor(ui): <主题>`

> ⚠️ 3.2 注意事项（项目既有踩坑记录）：Pinia 4 的持久化配置用 `pick` 而非 `paths`；store action 名不得与 api 方法同名（会爆栈）；`storeToRefs` 不解构 action。

---

### Phase 4 — 后端高风险模块（放最后，逐个单独提交）

| 批次 | 模块 | 风险点 |
|---|---|---|
| 4.1 | `config` | 读写 JSON 配置文件 |
| 4.2 | `pomodoro` | fs + DB 混合 |
| 4.3 | `library` | 磁盘 Markdown 读写 + cheerio 抓取，需拆 `services/library.service.ts` + `services/web-clip.service.ts` |
| 4.4 | `mindmap` | 本地 JSON 存储 + AI 双入口 |
| 4.5 | `reviews` | 旧 `wb_review_card` 体系 |
| 4.6 | `review` | **调用 `sm2.ts`——算法文件绝对只读**，只搬运调用点 |
| 4.7 | `inbox` | 897 行 + multipart 上传 + 状态大小写互转，建议内部再拆 3 个 service（inbox / upload / dedupe） |
| 4.8 | `ai` | 1130 行 + axios 第三方调用，需拆 `services/ai/*.service.ts`，LLM 客户端保持在 `lib/llm.ts` |
| 4.9 | `migration` | 一次性迁移逻辑，**最后动或明确豁免** |

---

### Phase 5 — 收口与文档同步

| # | 动作 |
|---|---|
| 5.1 | 全量 `tsc --noEmit` + `vue-tsc --noEmit` + `npm run build:all` |
| 5.2 | `npm run tauri:build` 冒烟（验证打包链路未受影响） |
| 5.3 | 契约终检：`diff -r before/ after/` 为空 |
| 5.4 | 同步更新 `技术架构与功能手册.md`、`README.md` 的目录结构与分层说明 |
| 5.5 | 删除 `.refactor-baseline/` 与临时脚本（或加入 `.gitignore` 长期保留） |

---

## 四、批次依赖与风险总览

```
Phase 0 安全网 ──┬─→ Phase 1 captures 试点（范式确立，需你确认）
                 │        │
                 │        └─→ Phase 2 小模块 ×8 ──→ Phase 4 高风险 ×9 ──→ Phase 5 收口
                 │
                 └─→ Phase 3 前端 ×6（与后端无耦合，可穿插）
```

风险分级：
- 🟢 低：Phase 0、2.1–2.5、3.1、3.2
- 🟡 中：2.6–2.8、3.3、3.4、4.1–4.4
- 🔴 高：4.5–4.8（SM-2 / 上传 / 第三方 API），必须单独提交 + 单独回归

---

## 五、红线自检表（每批提交前逐条打勾）

- [ ] 未改 `src-api/src/db/schema.ts` 任何列名/默认值
- [ ] 未改 `index.ts` 的 `onSend` 信封钩子
- [ ] 未改 `services/sm2.ts` 一个字符
- [ ] 快照 `diff` 为空
- [ ] 路由文件内无 `drizzle-orm` / `db.` / `axios` / `fetch(`
- [ ] 新增代码 0 个 `any`、0 个 `@ts-ignore`
- [ ] `tsc --noEmit` / `vue-tsc --noEmit` 均 0 error
- [ ] 已 `git commit`，**未** `git push`

---

*本清单在执行过程中随批次推进更新勾选状态。*
