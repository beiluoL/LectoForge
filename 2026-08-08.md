
## 待复习清单抽屉点击无反应 — Bug 修复（00:38 后）
- **现象**：点「待复习清单」/「清单」按钮无任何反应，抽屉不出。
- **排查**：静态链路全通（按钮 `@click=openQueueList`、store 置 `queueListVisible=true`、组件 `<Teleport>` 已挂载、`openQueueList` 确为 review store 实例）。起 vite(5173)+后端(8787)用无头 Chrome 实点，抓到关键日志：`[DBG] openQueueList after=true` 且 `ReviewQueueList queueListVisible->true`，但 `visible` computed 的 watch **不再触发** → `visible` 恒为 false。
- **根因（Vue 经典坑）**：`ReviewQueueList.vue` 的 `visible` 用 `modelValue === undefined ? store.queueListVisible : modelValue` 区分受控/非受控。但 `modelValue` 声明为 `boolean` prop，父组件不传时 **Vue 默认解析成 `false`（不是 `undefined`）**，于是判断恒走 `modelValue` 分支返回 `false`，store 开关联信号被完全无视。
- **修复**：删掉有缺陷的 v-model 双模式（当前无父组件用 v-model 控制它），`visible` 直接 `= queueListVisible`，`close()` 统一 `store.closeQueueList()`。提交 `fdd05c0`。
- **经验**：① Vue 中 `boolean` prop 不传即默认 `false`，不能用 `=== undefined` 判断是否被父组件显式控制；需要"受控/非受控"语义时应用独立 `Boolean` prop（如 `controlled`、`forceOpen`）而非复用 `modelValue`。② 此类"点击无反应"用无头 Chrome 实点 + 在 store action 与 computed 里插 `console.log` 最快定位，比纯读代码高效。
