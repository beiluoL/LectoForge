# LectoForge 桌面端 ↔ iOS 端 数据互通技术方案

> 状态：方案设计（暂不开发）
> 版本：v1.0.0-draft
> 适用：桌面端 `desktopApp`（Tauri 2 + Vue3 + Fastify/better-sqlite3）、iOS 端 `lectoforge_mobile`（Flutter/Dart + sqflite）
> 作者：WorkBuddy
> 日期：2026-08-14

---

## 0. 路径与前提校正

- 用户原始给出的 iOS 工程路径为 `/Users/beiliao/...`（typo）。**实际正确路径为 `/Users/beiluo/Documents/alProject/qoderProject/lectoforge_mobile`**（macOS 用户目录为 `beiluo`，非 `beiliao`）。本方案所有分析均基于该真实路径。
- iOS 端**实为 Flutter 工程**（Dart，可同时出 iOS/Android 包），并非原生 Swift。下文中“iOS 端 / 移动端 / Mobile”同义。

---

## 1. 最优方案建议（先读本节）

### 1.1 结论

**采用「方案 B：新增一套版本化中性交换格式 + 双边导入/导出接口」，并以现有备份 zip 机制作为传输载体；不推荐纯“直接复用备份文件导入”（方案 A）。**

即：在桌面端与移动端各新增 Export / Import 能力，二者之间约定一份**与内部 schema 解耦的中性交换格式（NDJSON + manifest + 文档库文件目录）**。桌面端已有的 `backup.js` 归档能力与移动端规划中的一键备份（P0 待做）**降级为“打包/解包”的传输层**，真正的语义转换由交换格式完成。

### 1.2 为什么不是「直接复用备份导入」

桌面端备份（`src-api/backup.js` + `src-api/src/services/backupService.ts`）产出的是：

```
lectoforge-backup-YYYYMMDD-HHmmss.zip
├── db/workbench.db (+ -wal / -shm)     ← 桌面端原生 SQLite 整库快照
├── uploads/                            ← 录音/图片/附件
├── config/                             ← ai-config.json / config.json
├── mindmaps/                           ← 桌面端导图散 JSON 文件
└── backup-meta.json
```

直接把这份 zip 的 `workbench.db` “塞进”移动端存在**四类硬伤**，任何一项都足以否决纯文件复用：

1. **Schema 漂移（致命）**：移动端 `lectoforge.db` 的 `_kTables`（`lib/database/database_helper.dart`）与桌面端 `db/schema.ts` 并非同构——移动端缺 `user_id/category_id/tags` 等列、缺整套规划域表；桌面端缺 `wb_mindmap/wb_quiz/wb_interview_*`；且 `wb_embedding.entity_id` 两端类型相反（桌面 `TEXT` / 移动 `INTEGER`）。直接挂载会导致列缺失 / 多余列 / 类型冲突。
2. **文档库丢失（致命）**：桌面端文档库是**文件型 vault（`.md` 真实文件）**，经 `src-api/src/lib/vault.ts` 读写，**根本不在 `workbench.db` 内，也不在备份 zip 内**（backup.js 仅打包 db/uploads/config/mindmaps）。纯复用备份会把“文档库内容”整体漏掉。
3. **SM-2 算法已分叉（致命）**：两端复习排程算法不一致（详见 §5.3），即便列结构对齐，同一张卡在两端算出的 `due_date/ease_factor` 会持续发散——数据“看起来通了”，复习进度实则失同步。
4. **版本与迁移体系不同**：移动端用 `_kVersion` + 两段式 `CREATE TABLE IF NOT EXISTS` + `ALTER TABLE ADD COLUMN` 幂等迁移；桌面端用 Drizzle schema + `db/index.ts` 迁移。两套迁移不可互相套用。

> 一句话：**备份文件是“给同一台机器/同一应用恢复用的整库快照”，不是“给另一个应用导入用的数据契约”**。它适合做传输载体，不适合做语义交换格式。

### 1.3 必须前置完成的“对齐三件套”（方案 B 的前提）

在打通任何数据互通前，先消除三处已存在的分叉，否则互联互通会带病上线：

| 编号 | 前置项 | 现状差异 | 处理 |
|------|--------|----------|------|
| P1 | **`wb_embedding.entity_id` 类型统一** | 桌面 `TEXT`（文档存相对路径串，其余存数字 id 字符串）/ 移动 `INTEGER` | 移动端改 `INTEGER → TEXT`，与桌面对齐；文档类 embedding 以相对路径为 id |
| P2 | **SM-2 算法统一** | 桌面 `sm2.ts` 把 quality 钳制到 **0..3** 且套 5 分制公式（EF 恒降，已知缺陷）/ 移动 `sm2.dart` 用 **0..5** 正确公式 | 两端同步修正到同一套（建议 0..5 + EF 下限 1.3 + 初始 2.5），存量卡 EF 统一回抬 |
| P3 | **文档库纳入交换包** | 桌面备份 zip 不含 vault；移动端 vault 也是 `.md` 文件 | 交换格式显式包含 `vault/` 目录，两端各自把 `.md` 落回自己的 vault 根 |

### 1.4 方案 B 的核心价值

- **抗漂移**：内部 schema 任意一侧演进，只需改该侧的“交换适配器”，不影响对端。
- **可增量**：中性格式天然支持 `updated_at` 比较的增量同步（未来可升级为实时云同步）。
- **保文档**：显式携带 vault 文件，文档库不再丢。
- **可校验**：manifest 带 `formatVersion`、各模块记录数、校验和，导入前先验证完整性。

---

## 2. 现状与约束

### 2.1 桌面端（数据侧）

| 项 | 内容 |
|----|------|
| 容器 / 运行时 | Tauri 2 + Vue3/Vite；Node(Fastify) 侧车，`src-api` 跑 `better-sqlite3` + Drizzle |
| 主库 | `<dataDir>/workbench.db`（SQLite，WAL 模式） |
| 关键文件 | `src-api/src/db/schema.ts`（15+ 表）、`src-api/backup.js`、`src-api/src/services/backupService.ts`、`src-api/src/lib/vault.ts`、`src-api/src/services/sm2.ts` |
| 备份 | `POST /api/backup` → spawn `backup.js` → 时间戳 zip（见 §1.2） |
| 文档库 | **文件型**：`<dataDir>/vault/` 下真实 `.md`；向量检索走 `wb_embedding`（doc 的 `entity_id` 为相对路径串） |
| 模拟面试 | 统一库 `wb_qa_bank`（`source_type`: import/review_card/note） |
| 规划域 | `wb_task`/`wb_task_list`/`wb_habit`/`wb_habit_log`/`wb_quadrant_task`/`wb_calendar_event` 等（移动端暂无） |
| 交集事实 | 时间戳统一 ISO8601 文本；逻辑外键（仅存关联列，无物理 FK）；SRS 列 `ease_factor` 整数 ×100 |

### 2.2 移动端（数据侧）

| 项 | 内容 |
|----|------|
| 容器 / 运行时 | Flutter/Dart（iOS+Android）；`sqflite` 直连 SQLite，无后端进程 |
| 主库 | `lectoforge.db`（`DatabaseHelper.fileName`），`_kVersion = 8` |
| 关键文件 | `lib/database/database_helper.dart`（`_kTables` 全量列定义）、`lib/utils/sm2.dart`、`lib/repositories/*`、`lib/repositories/file_system_repository.dart` |
| 备份 | **尚未实现**（跨端待开发清单 P0：`/backup` 一键 zip + 每日定时） |
| 文档库 | **文件型**：`file_system_repository.dart` 直接 `dart:io` 读写设备 `<docsDir>/<vault>/xxx.md` |
| 导图 | **表内**：`wb_mindmap.nodes` 存整棵大纲树 JSON（桌面端是散 JSON 文件） |
| 自测/面试 | `wb_quiz`（AI 自测）、`wb_interview_bank`（快递/口试题，来源 import）、`wb_interview_session` |
| 交集事实 | 与桌面端同一套跨端契约（手册 §13）：ISO8601、逻辑外键、`ease_factor ×100`、表名对齐桌面 `db/index.ts` |

> 两端已**设计层面**声明互操作契约（移动端 `跨端待开发清单.md` §3），但**实现层面尚未落地**，且已出现 §5 所列具体分叉。

---

## 3. 两种方案可行性评估

| 维度 | 方案 A：直接复用备份文件导入 | 方案 B：新增中性交换格式 + 导入导出接口（推荐） |
|------|------------------------------|-----------------------------------------------|
| 开发量 | 极小（仅移动端加一个“解压 db 并打开”入口） | 中（双边各加 Export/Import 适配层 + 一套交换 schema） |
| Schema 漂移 | ❌ 直接撞列缺失/类型冲突，需大量特判 | ✅ 适配层吸收差异，缺列填默认、多列忽略 |
| 文档库 | ❌ 备份不含 vault，文档整体丢失 | ✅ 交换包显式带 `vault/` |
| SM-2 分叉 | ❌ 排程持续发散 | ✅ 配合 P2 统一算法后，交换归一化 SRS 状态 |
| 导图 | ⚠️ 桌面散文件 ↔ 移动表结构需转换 | ✅ 适配层做 JSON↔表 互转 |
| 增量/双向 | ❌ 只能整库覆盖，无冲突策略 | ✅ 基于 `updated_at` 的 last-write-win，可双向 |
| 未来云同步 | ❌ 无扩展点 | ✅ 中性格式即云同步的本地雏形 |
| 版本演进 | �️ 任一侧改表即破 | ✅ `formatVersion` 解耦 |
| 结论 | **不可行**（仅能做一次性“同构库”恢复，不能跨应用） | **可行且是正确长期解** |

**判定**：方案 A 仅在“移动端复刻桌面端整套 15+ 表 schema”的极端前提下才勉强可用——而这违背移动端“规划域零覆盖”的产品定位，且仍绕不开文档库与 SM-2 两处致命项。故否决 A，采用 B。

---

## 4. 数据对齐策略

### 4.1 对齐总原则（沿用既有跨端契约）

- 时间：一律 ISO8601 文本（`updated_at` 作为冲突仲裁键）。
- 外键：逻辑外键，只搬关联列（`capture_id/note_id/palace_id/...`），不迁物理约束。
- SRS：`ease_factor` 整数 ×100、`repetitions`、`interval_day`、`lapse_count`、`review_count`、`due_date`、`last_reviewed_at` 作为标准字段透传。
- 表名：与桌面端 `db/index.ts` 对齐；移动端独有表在交换格式中以独立模块名出现。
- 集合/数组字段（`tags`、`options`、`source_filter`、导图 `nodes`）：统一序列化为 JSON 文本。

### 4.2 共享表对齐矩阵（核心学习闭环）

> 记号：D=桌面端列，M=移动端列。✅ 两端同义；➖ 仅一端有；⚠️ 类型/约束分歧。

| 表 | D 独有列 | M 独有列 | 分歧点 |
|----|----------|----------|--------|
| `wb_capture` | `user_id`,`doc_id`,`category_id`,`cover_image`,`processed_at` | — | `source_type` D 可空 / M `NOT NULL 'MANUAL'`；`content` D 可空 / M `NOT NULL ''` |
| `wb_note` | `user_id`,`category_id`,`tags` | — | — |
| `wb_review_card` | `user_id`,`capture_id`,`note_id`,`category_id`,`card_type`,`review_count`,`last_review_time` | — | `next_review_time` D `NOT NULL`(无默认) / M 默认 `'1970...'` |
| `wb_review_log` | `user_id` | — | `cost_ms` D 可空 / M `NOT NULL 0` |
| `wb_palace` | `user_id`,`description`,`category_id` | — | `theme` D 可空 / M `NOT NULL 'ROOM'` |
| `wb_palace_loci` | `user_id`,`category_id` | — | `knowledge_point` D 可空 / M `NOT NULL ''` |
| `wb_recall_session` | `user_id`,`note_id`,`card_id`,`title`,`completed_time` | — | `round*_text` D 可空 / M `NOT NULL ''` |
| `wb_story` | `user_id`,`capture_id`,`note_id`,`category_id`,`title` | — | `clarity_score`/`word_count` D 可空 / M `NOT NULL 0` |
| `wb_pomodoro_log` | `user_id` | `updated_at` | — |
| `categories` | — | — | `parent_id` D `NOT NULL 0` / M 可空 |
| `wb_embedding` | — | — | ⚠️ **`entity_id` D `TEXT` / M `INTEGER`**（见 P1） |

**搬运规则**：
- 导出方输出**列的并集**（多出的列照写）；导入方只认自己认识的列，未知列忽略，缺失的可空列留空、`NOT NULL` 列用默认值（与 `_kTables` / Drizzle default 一致）。
- `user_id`：桌面端单用户恒为 `1`；移动端无此列 → 导入移动端时丢弃，导出移动端时补 `1`。
- `category_id`/`tags`：移动端当前无 → 导入移动端时丢弃（移动端分类维度暂未实现，待补齐后回迁）。

### 4.3 存储模型分歧的处理

| 概念 | 桌面端 | 移动端 | 交换处理 |
|------|--------|--------|----------|
| 思维导图 | `<dataDir>/mindmaps/<id>.json` 散文件 | `wb_mindmap(id,title,nodes JSON)` | 导出时把表行 ↔ 散文件互转；统一节点形状 `{id,content,children[]}` |
| 文档库 | `<dataDir>/vault/**.md` 文件 | `<docsDir>/<vault>/**.md` 文件 | 作为 `vault/` 目录整体搬运，保留相对路径；落盘后两端各自重建 `wb_embedding` 索引 |
| 模拟面试/自测 | `wb_qa_bank`（统一库） | `wb_quiz` + `wb_interview_bank` + `wb_interview_session` | `wb_interview_bank` → `wb_qa_bank(source_type='import')`；`wb_quiz` → `wb_qa_bank(source_type='note', source_id=note_id)` 或单列模块；`wb_interview_session` 移动端独有（桌面端面试会话未持久化，单向迁移） |

### 4.4 ID 与冲突策略

- **本地自增 ID 不可跨端直接复用**（两端都可能 `id=5` 指向不同实体）。
- 交换记录携带**原始 `(platform, localId)`**，导入方落库后生成本地新 id，并在“导入映射表”记录 `{platform, localId → newLocalId}`，保证引用（`capture_id→note` 等）能在目标端正确重指。
- **冲突仲裁**：以记录级 `updated_at`（ISO8601，字典序即时间序）做 last-write-win；同则可结合 `created_at` 与平台优先级兜底。同一记录两端都存在时，取较新者，旧值进入“待人工复核”暂存（可选）。
- 首版建议**全量覆盖式导入 + 预检差异报告**，而非自动 merge，降低误覆盖风险。

### 4.5 文档库与向量索引

- `vault/` 文件按相对路径搬运；导入后：
  - 桌面端：重新跑 `embeddings/sync` 把 `.md` 向量化写入 `wb_embedding`（此时 `entity_id` 已统一为 TEXT 路径）。
  - 移动端：重建本地 embedding（移动端 `wb_embedding` 改 TEXT 后同逻辑）。
- 因 `wb_embedding` 是纯本地缓存（memory 亦记“重建索引即可重算”），**不强制搬运向量本体**，导入后重算最稳妥（也规避了 P1 类型冲突）。

---

## 5. 端到端数据流向图

### 5.1 文字版（导出 → 传输 → 导入）

```
[桌面端 Tauri]                        传输载体                           [移动端 Flutter]
  Export Service                         (zip / 云 / 直传)                  Import Service
  ├─ 读 workbench.db ─┐                                               ┌─ 解包 zip
  ├─ 读 vault/*.md   ├──►  lectoforge-exchange-<ts>.zip  ───────────► │─ 映射 ID / 冲突仲裁
  └─ 读 mindmaps/*    │     ├─ manifest.json (formatVersion,           ├─ vault/ → 本地 vault
        │             │     │   平台/版本, 各模块记录数, checksum)       ├─ data/*.ndjson → Repository
        ▼             │     ├─ vault/**.md                             └─ 重建 wb_embedding 索引
  [中性交换格式]       │     ├─ mindmaps/*.json
  (NDJSON/模块)       │     └─ data/<module>.ndjson
                      │                                                [落库 lectoforge.db]
  反向（移动→桌面）完全对称：移动端 Export 经同一 zip → 桌面端 Import。
```

### 5.2 交换包（zip）内部契约

```
lectoforge-exchange-<timestamp>.zip
├── manifest.json          # formatVersion, source{platform,appVersion,dbVersion},
│                          #   exportedAt, modules[{name,recordCount,checksum}]
├── vault/                 # 文档库 .md 文件（保留相对路径）—— 修复“备份丢文档”缺陷
├── mindmaps/              # 导图 JSON（每图一文件，或合并 mindmaps.ndjson）
└── data/
    ├── captures.ndjson
    ├── notes.ndjson
    ├── review_cards.ndjson
    ├── review_logs.ndjson
    ├── palaces.ndjson
    ├── loci.ndjson
    ├── recall_sessions.ndjson
    ├── stories.ndjson
    ├── embeddings.ndjson   # 可选；建议导入后重算
    ├── categories.ndjson
    ├── pomodoro.ndjson
    ├── quizzes.ndjson
    ├── interview_bank.ndjson
    └── interview_sessions.ndjson
# 每条 NDJSON = 一个归一化记录（列的并集 + _src{platform,localId}）
```

### 5.3 ⚠️ SM-2 算法分叉（必须统一，否则复习互通失效）

| 项 | 桌面端 `sm2.ts`（`gradeCard`） | 移动端 `sm2.dart`（`applySm2`） |
|----|-------------------------------|----------------------------------|
| 评分域 | 钳制 **0..3**（0 全忘 /1 有印象 /2 想起来 /3 秒答） | **0..5**（again0/hard3/good4/easy5） |
| EF 下限 / 初值 | 整数 ×100：MIN 130 / DEFAULT 250 | double：1.3 / 2.5 |
| EF 公式 | `ef/100 + (0.1-(5-q)(0.08+(5-q)*0.02))` | `ef + (0.1-(5-q)(0.08+(5-q)*0.02))` |
| 已知缺陷 | quality∈0..3 套 5 分公式 → **EF 恒降**，长期全沉到 1.30 | 公式正确（但仍与桌面域不同） |
| 间隔阶梯 | rep1=1, rep2=6, 之后 `round(interval×ef/100)` | rep1=1, rep2=6, 之后 `round(interval×ef)` |

**后果**：即便 `ease_factor/interval_day/due_date` 经列对齐搬运，因两端算法不同，导入后“下一次复习”会算出不同日期，复习闭环在两端渐行渐远。

**统一方案（二选一，须两端同步改）**：
- (a) 移动端把 0..5 映射到 0..3 再入桌面公式：`q = quality*5/3`（或反之桌面升到 0..5）；
- (b) 两端都改用 Anki 变体，按档查表加减（q=good 时 +0.15）。
- 无论哪种，**存量卡片 EF 需统一回抬到 DEFAULT_EF(2.5)** 消除历史沉底。

> 该分叉是“复习数据互通”的隐藏地雷，优先级高于导入导出接口本身（见 §1.3 P2）。

---

## 6. 模块级互通方案

### 6.1 核心学习闭环（collect → notes → review → active recall / Feynman）

```
收集箱(wb_capture) ──沉淀──► 笔记(wb_note) ──生成复习卡──► 复习(wb_review_card/loci)
                                                │
                                                ├─► 主动回忆(wb_recall_session：三轮闭卷默写)
                                                └─► 费曼故事(wb_story：audience/metaphor/gap_note)
       复习进度(SM-2) ◄──── wb_review_log(source_type: note|loci) ────┘
```

闭环内所有实体均经 §4.2 矩阵搬列；引用关系（`capture_id`/`note_id`/`palace_id`）经 §4.4 的 ID 映射重指。复习进度最关键：**先完成 P2(SM-2 统一)**，再搬 `due_date/ease_factor`，否则闭环在两端失步。

### 6.2 各模块互通要点

| 模块 | 互通方式 | 注意 |
|------|----------|------|
| **收集箱** `wb_capture` | NDJSON 直搬；`source_url` 两端都有，`doc_id` 仅桌面 | 移动端无 `doc_id`/`category_id`，导入丢弃；`source_type` 默认 `'MANUAL'` 兜底 |
| **笔记** `wb_note` | NDJSON 直搬；SRS 列随卡下沉 | `tags`/`category_id` 移动端暂无 → 暂丢，待移动端补分类后回迁；`title/cue/note/summary/image_hint` 全保留 |
| **复习** `wb_review_card` + `wb_review_log` | 卡 + 流水分别搬；`source_type` 区分 note/loci | **必须先 P2 统一 SM-2**；`review_count`/`last_review_time` 仅桌面有，移动端忽略 |
| **主动回忆** `wb_recall_session` | NDJSON 直搬（三轮文本+分数） | 桌面独有 `note_id/card_id/title/completed_time` 移动端无；移动端 `round*_text` 有默认值，桌面可空 |
| **费曼故事** `wb_story` | NDJSON 直搬 | `audience/metaphor/content/gap_note/clarity_score/word_count/status` 全保留；`capture_id/note_id` 引用走映射 |
| **文档库** | `vault/*.md` 文件目录搬运 + 导入后重算 `wb_embedding` | **列入交换包（修 P3）**；两端都是文件型 vault，天然契合 |
| **记忆宫殿** `wb_palace` + `wb_palace_loci` | NDJSON 直搬；loci 的 SRS 列同复习 | loci 的 `pos_x/pos_y` 画布坐标照搬；`category_id` 暂丢 |
| **思维导图** | 表↔散文件互转（`nodes` JSON ↔ `<id>.json`） | 统一节点形状 `{id,content,children[]}`；桌面 `markmap/vue-flow` 与移动端大纲树需对齐字段名 |
| **模拟面试/自测** | `wb_interview_bank`→`wb_qa_bank(import)`；`wb_quiz`→`wb_qa_bank(note)`；session 单向 | 移动端三表 ↔ 桌面统一库需字段映射；面试会话桌面端未持久化，仅移动→桌面单向 |

---

## 7. 互操作格式示例（manifest）

```json
{
  "formatVersion": 1,
  "source": {
    "platform": "desktop",
    "appVersion": "1.1.0",
    "dbSchema": "workbench@v15"
  },
  "exportedAt": "2026-08-14T01:00:00.000Z",
  "modules": [
    { "name": "notes",        "recordCount": 128, "checksum": "sha256:ab12..." },
    { "name": "review_cards", "recordCount": 340, "checksum": "sha256:cd34..." },
    { "name": "vault",        "recordCount": 56,  "checksum": "sha256:ef56..." }
  ],
  "notes": "SM-2 已统一至 v2 算法；embedding 导入后重算"
}
```

单条记录（NDJSON 示例）：

```json
{"_src":{"platform":"desktop","localId":42},
 "id":42,"capture_id":7,"title":"并发编程笔记",
 "cue_column":"什么是 happens-before","note_column":"...","summary_column":"...",
 "mastery":60,"due_date":"2026-08-20T00:00:00.000Z",
 "ease_factor":250,"repetitions":3,"interval_day":12,
 "lapse_count":1,"review_count":5,"last_reviewed_at":"2026-08-08T00:00:00.000Z",
 "tags":"[]","created_at":"2026-07-01T00:00:00.000Z",
 "updated_at":"2026-08-08T00:00:00.000Z"}
```

---

## 8. 实施路线图（建议，暂不开发）

| 阶段 | 任务 | 产出 |
|------|------|------|
| 0 | 前置对齐：P1 移动端 `entity_id→TEXT`；P2 统一 SM-2 + 存量 EF 回抬；P3 文档库纳入交换包 | 消除 §5.3 / §4.2 / §4.3 分叉 |
| 1 | 定义交换 schema（`manifest` + 各模块 NDJSON 字段并集 + 节点形状） | 本方案的 §7 落地为规范 |
| 2 | 桌面端：Export Service（读 DB+vault+mindmaps→zip）+ Import Service（zip→DB，含 ID 映射/冲突） | `src-api/src/services/exchangeService.ts` + 路由 |
| 3 | 移动端：Export（Repository→NDJSON+`vault/`+`mindmaps/`）+ Import（NDJSON→Repository） | `lib/services/exchange_*` + UI 入口 |
| 4 | 传输层：先支持“文件导出/导入”（AirDrop/Files/iCloud 落盘），预留云同步接口 | 双向可用 |
| 5 | 验证：用 `scripts/api-snapshot` 思路做“导出→导入→再导出”幂等与闭环一致性测试 | 回归证据 |

---

## 9. 风险与待决

- **R1（高）SM-2 分叉**：未在互通前统一，复习进度必失步。→ 阶段 0 强制前置。
- **R2（高）`entity_id` 类型**：不修正则文档 embedding 无法跨端索引。→ P1。
- **R3（中）分类/标签维度**：移动端暂缺 `category_id`/`tags`，导入会丢维度；需先决定“暂丢待回迁”还是“移动端先补分类”。
- **R4（中）导图字段语义**：桌面 `markmap/vue-flow` 节点属性（颜色/折叠/坐标）与移动端大纲树未必一一对应，需定义最小公共子集。
- **R5（低）大库性能**：`wb_embedding` 量大；建议交换包不搬运向量本体、导入后重算（§4.5）。
- **待决**：首版是否做自动 merge 还是全量覆盖 + 预检报告（建议后者）；传输是否一步到位上云同步（建议先文件，后云）。

---

## 附：关键代码锚点（供后续开发查阅）

- 桌面备份：`src-api/backup.js`、`src-api/src/services/backupService.ts`
- 桌面 schema：`src-api/src/db/schema.ts`
- 桌面文档库：`src-api/src/lib/vault.ts`、`src-api/src/services/libraryService.ts`
- 桌面 SM-2：`src-api/src/services/sm2.ts`
- 移动 schema：`lib/database/database_helper.dart`（`_kTables`、`_kVersion=8`）
- 移动文档库：`lib/repositories/file_system_repository.dart`
- 移动 SM-2：`lib/utils/sm2.dart`
- 跨端契约：`lectoforge_mobile/跨端待开发清单.md` §3
